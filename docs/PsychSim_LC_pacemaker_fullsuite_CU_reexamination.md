# Full-suite gate on the grounded LC pacemaker — 3 substantive failures = the CU re-examination arriving — finding (HOLDING commit)

The grounded LC pacemaker (baseline 0.15, electrophysiology-derived) **passes the ruling's specific test**
(paired-learning regression resolved + latch stays broken) and the full suite is **509/513**. But beyond the
authorized golden regen, **3 committed behavioural tests fail** — and 2 are the **CU punishment crux** the ruling
scheduled for re-examination "on the fully-clean mechanism." They are surfacing now, as the clean mechanism's
global tonic-NA lift changes the read-outs. **Not golden-regen material — holding for your steer.**

## Full-suite result: 509/513, 4 failures
1. `test_characterisation::matches_committed_baseline` — **golden**, authorized honest regen (coherent NA-tone
   shift: `defensive_threat` up, `affiliation`/`reward_approach` slightly down, orderings preserved).
2. `test_substrate_study::test_normal_newborn_acquires_some_aversion` — **CU punishment** ↓
3. `test_substrate_study::test_punishment_deficit_is_weak_not_a_failure` — **CU punishment** ↓
4. `test_substrate_behaviour::test_acting_readiness_peaks_in_adolescence` — dual-systems keystone ↓

## Failures 2–3: the CU punishment read-out is now CONFOUNDED by tonic-NA-into-defensive-output
`punishment_learning` = (defensive-output-to-cue **after** 60 cue+nociception pairings) − (**before**), over
`DEFENSIVE_OUTPUT = (CeA, PAG, BA)`. The graded profile inverted:

| throttle | HEAD | CLEAN mechanism |
|---|---|---|
| 0.0 (normal) | −0.013 | **−0.094** |
| 0.5 | +0.020 | −0.025 |
| 1.0 (throttled) | +0.040 | +0.040 |

Pinned to root (throttle 0.0): the LC pacemaker's **tonic NA lifts the `before` defensive baseline 0.341 →
0.456** (LC→CeA/BA, both DEFENSIVE_OUTPUT members); `after` is 0.362 (actually *higher* than HEAD's 0.328). So
the **difference** goes sharply negative — **not because aversion isn't learned, but because the read-out's
baseline is NA-inflated** (and habituates over the 60 trials as the phasic CeA→LC adapts). `punishment_learning`
(a difference of defensive-output levels) **cannot isolate the learned cue→defensive plasticity from the tonic
defensive tone.**

**This is precisely the CU re-examination you scheduled** — and the answer is not a clean persists/dissolves:
the **read-out itself is confounded** by the (now grounded, higher) tonic NA, the *same confound class you
originally flagged* (tonic-NA artifact), now living in the **measurement** rather than the teaching signal. The
phasic teaching fix cleaned the *learning gate*; it did not make the *defensive read-out* NA-robust.

## Failure 4: acting-readiness — a thin 1-step inversion
`teen.steps ≤ child.steps` (adolescent reaches approach at least as readily as a younger child). HEAD: child 37,
teen 35, adult 43 (pass). Clean: child 38, teen **39**, adult 47 — teen exceeds child by **1 step**. The tonic-NA
arousal lift nudged the adolescent's steps-to-approach up past the child's by a hair. Marginal, but it flips the
committed ordering.

## Why I'm holding (not regenerating/re-baselining unilaterally)
- The golden is authorized; these 3 are **committed behavioural assertions** (the CU study crux + a v9
  dual-systems keystone), and the ruling required "**full suite green; v9 + Phase-1 intact**." They're not.
- The punishment failures **are the deliberate CU re-examination** — you wanted to run it consciously, not have
  it silently regenerated. And the finding is substantive: the CU punishment probe is confounded by
  tonic-NA-into-defensive-output on the clean mechanism.
- Per your own method: the grounded LC value resolved the *specific* regression the ruling targeted
  (paired-learning + latch) — so I do NOT tune LC. But it has *global* consequences that break the CU crux, and
  **that is a finding to surface, not to absorb by regenerating the CU tests.**

## The decision I need
The clean mechanism is built and green except these 4. Options (your steer):
1. **Make the CU punishment read-out tonic-NA-robust** — isolate the learned cue→defensive plasticity from the
   NA-inflated/habituating tonic defensive tone (e.g. measure the cue-specific *increment* against a
   contemporaneous NA-matched baseline, or read the learned cue→CeA/BA *weight* change rather than the activation
   level). Then re-examine the deficit cleanly. (My lean — the read-out is the thing that's confounded.)
2. **Re-examine + re-baseline the CU tests deliberately** as the scheduled re-examination, documenting that on
   the clean mechanism the graded punishment profile is dominated by the NA-tone read-out confound (a finding).
3. **Reconsider** whether 0.15 is right (but it's electrophysiology-grounded, and the ruling said a
   non-resolving grounded value is a finding, not a tune — so I won't move it into a window).
4. The acting-readiness thin inversion: re-baseline (emergent from the grounded NA change, marginal) or treat as
   a concern?

Nothing committed. Tree = complete candidate (phasic CeA→LC + α2 moderate + LC pacemaker 0.15). **No result is a
target — both ways:** the grounded value greens the arc's core (latch/α2/aggression/paired-learning), but I'm
not regenerating the CU crux to force a green suite — the confounded punishment read-out is the substantive
finding your CU re-examination was meant to produce.
