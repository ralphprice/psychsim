# The auditory pass — **CLEARED** (`1c264a5`). Go on Q2.

**Verified:** 95 / 238. `IN-AUD → COCH` in; `COCH ← IN-AUD`, `COCH → AUD-brainstem, StN`. **The raw-channel
asymmetry is closed — all five modalities now have one** (`IN-VIS→RET`, `IN-OLF→OB`, `IN-VESTIB→VN`,
`IN-INTERO→NTS`, `IN-AUD→COCH`). **One edge, predicted mechanically by the input-surface audit, and the whole
dead subcortical relay lit.** Golden unchanged, bank compatible, `adults.json` byte-identical, one expected
red (the Q2 gap) that self-clears.

**And your §18 handling is exactly right — recorded:** *"startle's existence is solid; its magnitude is
provisional-upward."* **The existence is grounded** (a cited `COCH→StN→CeA` cascade that had no input now has
one); **the magnitude rides on a saturating `CeA` and eight unbraked cortical nodes.** **A first-ever result
is precisely where the temptation to bank a magnitude is strongest, and you didn't.**

---

## ★ There were TWO asymmetries. We closed one.

```
IN-VIS:face_like          -> SC-Pv     ← SUBCORTICAL
IN-VIS:biological_motion  -> SC-Pv     ← SUBCORTICAL
IN-AUD:voice              -> A1-belt   ← CORTICAL
```

**The RAW asymmetry is closed. The FEATURE asymmetry is not — vision's features enter subcortically,
audition's enters in cortex.** **And door 1's auditory half is blocked on THAT one, not on the raw channel.**
Your read ("the cry still bypasses") is right; this is the shape of why.

## The remaining diagnostic has two parts — and the first may not be in the seed at all

**(a) Does the Arena present the cry on the RAW channel?** In reality a cry is **both a sound and a voice** —
it reaches the cochlea *and* it is voice-like. **Phase C presents it on `IN-AUD:voice` only, so the agent
hears a voice without hearing a sound.** *(Check the visual side for the contrast: is a displayed face
presented on `IN-VIS` as well as `IN-VIS:face_like`? If it is, the auditory presentation is simply
inconsistent with it.)* **If the presentation is the gap, the fix is in the ARENA, not the seed** — a
one-line change with the entire subcortical chain already behind it.

**(b) Does the `IC` exist?** `AUD-brainstem`'s `function` names *"interaural sound localisation"* — **the
superior olive's job.** `IC → PGi` is cited and **may have no source node.** Grain question, not a naming one.

> **⚠️ And (a) ALONE DOES NOT CLOSE DOOR 1.** A cry reaching `SC-Pv → CeA → LC` is **still amygdala-obligate**
> — it would only give the auditory channel the same route the visual one already has. **The non-amygdala
> route is `IC → PGi → LC`. So door 1's auditory half needs (b), and (a) only feeds it.** Do not let (a)'s
> numbers look like a closure — that is the direct-pain 37% again.

---

## Go: **Q2 — `vlPAG`'s drive on the settled node**
It is what turns the freezing floor from an expected red into a held one — **and it is the first time freezing
becomes testable at all.** Both halves of that floor must pass on the mechanism.

**Then:** door 1's auditory half — **(b) first: the `AUD-brainstem`/`IC` grain, because (a) is worthless
without it.** Then the channel gaps (`PB-LOOMING` — the escape trigger the model cites and cannot fire ·
`PR-SALT` · `PR-HOMEOSTATIC` · `IN-PROPRIO` · predator odor). **And the census, with `MeA`'s three remaining
fallback edges at the top — `MeA → BNST` is the one that cannot be right at any single sign.**

**Door 1: visual `TRUE BY ANATOMY` (measured), auditory live. #2 open on doors 2 and 3.**
