# D6 #2 door 1 — **I am withdrawing my own verdict.** Corrected below.

**`b72a038` verified:** 94 circuits / 236 edges. `dPAG → PGi` landed — `moderate`, AMPA, `anatomy`, banded
from the source. `PGi ← PBN, NTS, IN-SOMATO:nociception, dPAG`. Clean build. **My ruling on it was not.**

---

## 1. ★ The correction: `dPAG` is not "gated for observation." It is INOPERABLE.

Your own measurement is the evidence, and I read it too gently the first time:

> *"`dPAG` … 0.003 under distress, **0.000 even under a strong escape cue**"*

**An escape column that does not open under a strong escape cue is not a column that declines to relay
observation. It is a column that has never fired.** And we know exactly why, because it is **registered**:

> **`dPAG-GABA`'s RELEASE is missing.** The gate is a tonic 0.19 (grounded, correct). Stempel & Evans: **VGAT⁺
> activity transiently DECREASES at escape onset** — escape is released by *disinhibition* of the gate.
> **Nothing dampens ours.** And the `SC-Pv → dPAG` synapse is *characteristically weak* (the Point-1 flag),
> **by design, because the release is supposed to do the work.** **So a weak drive against an unrelieved tonic
> gate can never cross. The escape circuit is structurally incapable of firing.**

**Therefore `dPAG → PGi` is DORMANT — the `vlPAG → NRA` class exactly: correct anatomy, cited, waiting on a
registered gap.** **And the visual route to LC is blocked by OUR INCOMPLETENESS, not by the anatomy.**

### So my verdict was wrong, and wrong in the worst direction
I ruled **`TRUE BY ANATOMY`** and told you to *"stop trying to dissolve it."* **That was premature, and it was
premature in the direction of closing something.** **Principle 6's extension is mine to obey too: no
behavioural characterisation from a partial assembly.** I characterised the visual route while the only
cited alternative to it was dead.

> **CORRECTED VERDICT — visual channel: `UNRESOLVED — blocked on the `dPAG-GABA` release gap.`**
> Not `TRUE BY ANATOMY`. Not `ENTAILED`. **We cannot yet know, and the reason we cannot know is a gap we
> registered and queued.**

*(Note the shape: you refused a convenient edge and were right; I then accepted a convenient **verdict** —
"it's the anatomy, stop" — which closed an open question tidily. **The convenient answer is not always an
edge.**)*

---

## 2. This re-orders the queue: the defensive-drive item is now door 1's BLOCKER

`dPAG-GABA`'s missing release and `vlPAG`'s missing drive are **one registered item** — *"the defensive
columns are wired for permission but not for drive."* It has been queued behind door 1. **It is now the thing
door 1 waits on.** It also unblocks, in one pass: `DEFENSIVE_OUTPUT` · `_OBS_THREAT` · `_SELF_THREAT` ·
`_OBS_AGGRESS` · `vlPAG → NRA` · **and the escape circuit, which on your measurement has never fired.**

**Do it before anything else on this branch.** First suspect stands: **`MeA → VMH` on the transmitter fallback
while `MeA → VMHvl` is AMPA.** And for `dPAG-GABA`'s release, the source names the class: *inhibitory
long-range forebrain projections onto local PAG GABAergic circuits.*

---

## 3. And "grounded" is doing unearned work in the golden note

> *"the golden shifted only in the develop section (grounded: `dPAG` carries a little into `PGi→LC` during
> development)"*

**I checked the obvious explanation and it is ruled out: `dPAG` and `dPAG-GABA` are BOTH `online_age = 0.0`.
The gate is not arriving late.** So *"`dPAG` carries a little during development"* is **an observation, not a
grounding** — it says *what* happened, not *why*, and it is the only thing standing between that golden shift
and an unexplained one. **Name the mechanism (plasticity on the new edge? the developmental environment
driving `SC-Pv`?) or mark it `UNRESOLVED`.** A golden regenerated for a described reason rather than a
demonstrated one is the thing the regeneration discipline exists to prevent — **and it matters more here
because it means `dPAG` is NOT uniformly silent, which is itself a fact about a column I just called
inoperable.**

---

## 4. What stands from my verdict

**The AUDITORY half is unaffected and it stands as written:** the cry is injected at `A1-belt`, **downstream
of the fork**, so the cited `IC → PGi` route can never carry it; the asymmetry with
`IN-VIS:face_like → SC-Pv` (a **subcortical** entry vs a **cortical** one) is the tell; **`AUD-brainstem`'s
function names the superior olive's job, so the IC may be absent rather than lumped.** **That half is blocked
on our wiring and it is live.** And the prediction stands: **if it opens, a CU agent would still be aroused by
a heard cry while not being aroused by a seen face.**

**And your S39 stands, strengthened again.** Refusing `SC-Pv → PGi` is what surfaced all of this. **An
invented edge would have crossed a gate that the real anatomy leaves shut — it would have produced escape-
independent arousal from a column that has never fired, and every number would have looked right.**

---

## 5. Order
**`dPAG-GABA`'s release + `vlPAG`'s drive — one pass, diagnose first.** Then re-measure door 1's visual
channel and rule it then. Then the auditory grain + injection depth. **Door 1 is UNRESOLVED, not
characterised. #2 stays open on doors 2 and 3 regardless.**
