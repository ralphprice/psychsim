# The facial drive — RULING

**Report accepted in full. Three of these are bigger than the phase, and one of them is the most disciplined
thing in this arc.** Build the ruling below; the rest is registered.

**First, the win, before the reds bury it:** `BA → dACC` was cited to Morecraft, banded from the literature's
own word ("robust"), **measured after** — and it produced **`separation drives the voice; pain drives the
face`**, with **vicarious weaker than direct, emergent**. *Nobody wrote that.* A modality dissociation fell
out of two grounded edges. That is what the substrate is for.

---

## 1. `dACC-GABA` — YES, build it. But my framing was wrong, and the truth is worse.

**Your reasoning is right and I am not going to improve on it:** three excitatory afferents, no local
inhibition; the node only stayed quiet because its afferents were inert; **and the fix is not lowering
`BA → dACC` to "low" — that is rebalancing a real cited edge to compensate for missing anatomy, which is the
DRN-GABA error exactly.** Principle 1. Build the missing element.

**I went to check that `dACC` was the one cortical node missing its interneuron — a tidy story where v13 had
completed a layer and left one gap. It is not true.** Only **`dlPFC` and `vmPFC`** have `-GABA` partners.
**`dmPFC`, `vlPFC`, `OFC`, `dACC`, `aIns`, `mIns`, `pSTS`, `rSMG-TPJ`, `PCun-PCC` have none.**

> **The cortical inhibitory-interneuron layer is 2 of 11. `dACC` is not the last gap — it is the first
> landmine we have stepped on.**

**And your own sentence is the general law:** *"It only stayed quiet before because its afferents were
inert."* **Eight more cortical nodes are unbraked and quiet for exactly that reason. Every one of them will
saturate the moment we give it real drive** — and we are about to give several of them real drive
(`M1-face`, `PMC-l`, the whole Phase D limb; the representational layer above that).

**BUILD `dACC-GABA` now** — it is on the path and load-bearing. **Rate grounded from cortical FS/PV
electrophysiology BEFORE looking at what restores the tests; setpoint paired; and if the grounded rate does
not restore them, that is a FINDING** (the LC / `dPAG-GABA` ruling, third application). **REGISTER the other
eight as a systemic condition** — not a to-do list, a property of the substrate: **§18, the cortical brake
layer is 82% absent.**

---

## 2. ★ The divergence — your handling of this is the best thing in the arc

> *"That is the single result this thesis would most like to see, and it is an unbraked node saturating the
> outcome measure into its ceiling. It arrived with its own artifact already identified."*

**Correct, correct, and recorded. The earned negative is NOT retired on this basis.** You found the result
the thesis most wants and identified its artifact in the same breath, before anyone asked. That is principle
3 working in the only direction that is hard.

**And it exposes something we should have seen after the CU signature — take it as a standing caution:**

> **MISSING INHIBITION DOES NOT JUST DESTABILISE. IT MANUFACTURES EFFECT SIZES.** An unbraked node saturates;
> saturation drives the outcome measure into its ceiling; a ceiling looks like a large effect. **A substrate
> with 2 of 11 cortical brakes is biased toward producing large, clean, publishable-looking effects — in the
> direction we want.** The CU punishment "signature" (dead LC), and now the divergence (unbraked dACC): **both
> times the wanted result appeared exactly where a mechanism was incomplete.** That is not coincidence — it is
> the mechanism of the artifact.

**Consequence to register: every effect SIZE measured on this substrate to date is provisional in a specific
direction — upward.** Not the existence of the effects; their magnitude. **That belongs in §18 with the audit
caveat.**

---

## 3. The executive read-out — you are right, but do not re-express it yet

`dACC` is *"conflict/error monitoring"*; **rising under a harsh unpredictable environment is correct
behaviour, not a failure.** The test wanted control capacity. The read-out conflates **monitoring** with
**control** — the `AFFECTIVE_EMPATHY` shape, in a read-out set.

**And your other half is the sharper half:** *"the old pass was thin — `OFC` alone carried warm>harsh while
`dlPFC` slightly favoured harsh and `dACC` sat flat. Same shape as Phase A: a passing test certifying the
outcome, not the mechanism."* **You applied the Phase A principle to a new case and it held. That is the
principle earning its keep.**

**RULING: do not re-express now.** Build `dACC-GABA`, re-run, then look. A read-out re-expressed against a
partial assembly re-expresses the artifact. **If it still conflates once the brake is in, re-express it —
and the re-expression must separate monitoring from control, which are different constructs, not two terms
of one.**

> **REGISTER — this is now a class, not an instance.** Two read-out sets are known to rest on a subset of
> their terms: **`DEFENSIVE_OUTPUT`** (2 of 3 — §5) and **the executive read-out** (`OFC` alone).
> **A READ-OUT AUDIT is now as necessary as the throttle-set audit, and it is the same check from the other
> side: does every term of the set actually carry?** Queue with the 209-edge audit.

---

## 4. The `pSTS` correction — I was wrong, and your correction makes the ruling stronger

*"Not by any path"* was too strong. `BA → pSTS → rSMG-TPJ → aIns → dACC → NuFac` existed. **My check traced
the direct route and I reported the absence as total. Take the correction.**

**But what you found in its place is the fourth construct-validity catch of this arc and the worst of them:**

> **`pSTS` and `rSMG-TPJ` are `COGNITIVE_MENTALIZING` — the amygdala's ONLY access to the face ran through the
> network CU SPARES. Unbuilt, a CU agent's face would have kept moving.**

**The model would have produced the OPPOSITE of the psychopathy expression signature — blunted mimicry is the
finding; intact mimicry is what the wiring would have given — and it would have been by construction, from a
routing nobody chose.** Nothing was aimed; the artifact was in the topology. **This is why construct validity
is a separate standing check and not a corollary of "no result is a target."** Recorded.

---

## 5. ★ The freezing column — confirmed, and there is an immediate suspect

**Verified: `vlPAG ← vlPAG-GABA (⊣), VMH, DRN (5-HT1A, ⊣)`. Its one excitatory afferent is `VMH`. And
`VMH ← MPOA (GABA-A, ⊣), MeA (FALLBACK)`. So `VMH` has no excitatory afferent, and `vlPAG` reads 0.000.**

**Your sentence is the principle and it should be recorded verbatim:**
> **"Disinhibition only permits; something must drive."**

We built the gate architecture beautifully onto a column with nothing to release. **Fourth instance, and the
sharpest variant: wired only in the sign that cannot start anything.**

**⚠️ THE IMMEDIATE SUSPECT — check this first, it may be the whole answer:**
**`MeA → VMH` is on the transmitter FALLBACK (`dominant_receptor: None`), while `MeA → VMHvl` is AMPA.**
**Same source, two targets, one decided and one not.** MeA is heterogeneous — glutamatergic *and* GABAergic
populations — so the fallback is not merely undecided, **it may be silently inverting the one edge that would
drive the freezing column.** **Ground it. Do not guess it, and do not assume symmetry with VMHvl.**

**RULING: register, do not build.** It is pre-existing, the lump hid it, and it is a defensive-circuit
question, not an expression one. **Merge it with `dPAG-GABA`'s missing release — they are one finding: the
defensive columns are wired for permission but not for drive.** One pass, when it is reached.

**`vlPAG → NRA` is not an error — it is GROUNDED AND DORMANT**, exactly like `PBN → LC`. Leave it. It will
carry when the column has drive.

**And the caveat is heavy: `DEFENSIVE_OUTPUT = ("CeA", "vlPAG", "BA")` has been computing on 2 of 3 terms.
Every freezing and defensive-output result to date is provisional in that specific sense.** Register it with
the audit caveat; it is the same class.

---

## Build, then stop
`dACC-GABA` only — grounded rate first, setpoint paired, then re-run and **report what holds and what does
not.** Nothing else. **Do not regenerate the golden, do not re-express the read-out, do not touch
`BA → dACC`'s band, and do not retire the earned negative.**
