# The defensive-drive pass — GROUNDING

**Both halves answered. Half 1's answer is much bigger than the question, and it touches the keystone again.**

**First — your refinement is accepted and my "inoperable" is withdrawn.** `dPAG` fires for **attack**
(`VMHvl → dPAG` at 0.85 crosses) and not for **flight** (the weak `SC-Pv` synapse cannot). **That is the dl/l
lump doing exactly what a lump does — the node's behaviour is the union of two columns — and it grounds the
golden shift properly: `develop()`'s provocation curriculum fires the *lateral* arm.** An observation became
a mechanism. Good.

---

## HALF 1 — `MeA → VMH`: **glutamatergic. The fallback is backwards.** And that is the small half.

> **"MePV cells that project to the ventromedial hypothalamus (VMH) are GLUTAMATERGIC neurons and increase
> their activity in response to CAT ODOR in mice."**
> **"The MeA and MePV project to key structures of the circuit involved in the defensive response against
> predators (… dorsomedial aspect of the ventromedial hypothalamic nucleus)."**

**Our `VMH` is the defensive/predator one. Its MeA input is glutamatergic. The fallback picked GABA, and that
is why `VMH` is dead and the freezing column has no drive.** Your suspect is confirmed, and **you were right
not to assume symmetry with `VMHvl`'s AMPA** — for a reason neither of us anticipated:

### ★ The `MeA` is LUMPED — the TENTH — and the fallback's ambiguity IS the lump declaring itself

> **"projections from the MeApd to the VMH … are largely GABAergic and segregated from those of the MeApv,
> which are mostly glutamatergic."**
> **"the MePD projects mainly to structures that control REPRODUCTIVE behaviors [… ventrolateral aspect of
> the VMH]"** — while **MeA/MePV → the dorsomedial VMH, for predator defence.**

| subnucleus | transmitter | target | function |
|---|---|---|---|
| **MePV** | **glutamatergic** | **VMHdm** (our `VMH`) | predator defence — cat odor |
| **MePD** | **GABAergic** | **VMHvl** (our `VMHvl`) | reproduction / aggression |

**Opposite transmitters, opposite targets, opposite functions — one node.** The seed's `MeA` cannot be both,
and **the transmitter field literally says "GABA/glutamate," which is not an ambiguity: it is the lump,
written down, and the fallback resolving it by coin-flip.**

### ⚠️ ★ And `MeA → VMHvl` (AMPA) is probably ALSO wrong — and it is the `CeA→PAG` story again
**MePD → VMH is GABAergic.** And the VMH itself: *"a **core** region that contains the nucleus's
glutamatergic projection cells, and a cell-poor **shell** region … mostly populated by **GABAergic cells,
which are thought to inhibit core neurons**."*

> **So the aggression route is very likely `MeApd (GABA) → VMH shell (GABA) → disinhibits the core`. The same
> disinhibition architecture as `CeA → vlPAG-GABA`. Our AMPA produces the right OUTCOME by the wrong
> MECHANISM — which is exactly what the v9 keystone flip found, and this edge feeds the aggression keystone.**

**And `VMH` is lumped too — the ELEVENTH: core (glutamatergic, projecting) + shell (GABAergic, gating).**

**RULING: diagnose, do not build.** The `MeA → VMH` sign flip is grounded and available — **but it is one
edge of a lumped node whose other edge is probably also wrong, and the other edge is keystone-bound.**
**Bring the MeA split and both edges as one diagnostic.** Fixing one sign inside a lump is how we got here.

### Your Q2 — what drives `vlPAG` once disinhibited: **I do not have it.** Honest.
It needs its own pass. **But your counterfactual's premise has now changed: you flipped a sign inside a lump.
Re-run it after the MeA/VMH grain is settled, not before** — VMH=0.116 was measured on the wrong node.

---

## HALF 2 — the escape release: **the literature does NOT name a source. `UNRESOLVED` — and that is an answer.**

Stempel & Evans establish **the dip** (*"VGAT⁺ activity transiently decreases at escape onset"*) and that
**optogenetically decreasing VGAT⁺ activity raises escape probability**. **They do not identify what causes
the dip.** And their sentence about *"inhibitory long-range projections from forebrain regions inhibiting
local GABAergic circuits within the PAG"* **cites work on vocalisation and freezing — the vlPAG/CeA case. It
is offered as an ANALOGY, not as a finding about dPAG.** **Do not invent the source.**

### ⚠️ And the premise needs correcting — the paper's causal claim is not ours
> **"excitatory VGluT2⁺ neurons receive information about imminent visual and auditory threats from the
> superior colliculus through a FEED-FORWARD EXCITATORY connection, and their activation determines the onset
> of escape and controls escape vigour."**

**Escape is driven feed-forward from the SC. The tonic gate SETS THE THRESHOLD. The paper does not claim
escape requires a release — "the gate must be released" is OUR inference.** So the model's failure has **two**
candidates:
- **(a)** a missing release — unresolved, above;
- **(b)** **the threshold is a GROUNDED baseline (0.19) read through TWO SCAFFOLD weights** —
  `dPAG-GABA → dPAG` ("strong") and `SC-Pv → dPAG` (0.50).

**(b) is checkable NOW, with no new anatomy — and it is a SENSITIVITY CHECK, not a tune:** sweep the two
scaffold bands across their plausible ranges and ask **"is there ANY setting at which a salient SC drive
crosses?"**
- **No** → a mechanism is missing and the release is it. **Register unresolved; do not guess it.**
- **Yes** → the bands are load-bearing, and **principle 2 applies: the arbitrariness is in a mechanism they
  compensate for.** Either answer is worth having; neither is a value change.

### 🎁 A relational grounding from the same paper — it may settle (b)
> **"these glutamatergic neurons receive input from local GABAergic dPAG neurons with a HIGH CONNECTIVITY
> RATE … a SUSTAINED BARRAGE of inhibition"** — **"in contrast with the SPARSE connectivity between VGluT2⁺
> dPAG neurons."**

**Dense inhibition, sparse excitation.** That is the gate taxonomy's lesson again — **what is groundable in
isolation is a RELATION, not a VALUE** — and it says our bands are directionally *right*, which makes (a)
more likely and the sweep more informative.

### 🎁 And one for the auditory question, free
**"the SC receives information about imminent VISUAL AND AUDITORY threats."** So `AUD-brainstem → SC-Pv` is
correct and load-bearing — **and Phase C's injection of the cry at `A1-belt` bypasses that too.** The
injection-depth finding is now doubly confirmed: **the cry cannot reach `SC-Pv` OR the IC as wired.**

---

## Order
**The `MeA`/`VMH` grain diagnostic (both edges, one pass, keystone-bound — surface before building)** → the
`dPAG-GABA` sensitivity sweep → then `vlPAG`'s drive as its own question. **Door 1 `UNRESOLVED`; #2 open on
doors 2 and 3.**
