# Vicarious routing — BUILT + measured as-found (close-out)

Per `PsychSim_Vicarious_Route_Ruling.md`. Routing built (Decision 2); **`vicarious < direct` NOT built**
(Decision 1 — neither a gain term nor a chosen intensity); the relationship **measured and reported as-found**
with its measurement limit; §4 recorded as structurally entailed; the accumulation verification **deferred**.

## What was built — routing only (`_add_consequence_percept`, arena.py)
The consequence that befell the other is now presented to a co-located perceiver **the same way the other's act
and physical endowment already are** — as a physical fact, through channels that already exist:
- Read the bearer's **evoked** distress — its defensive population (`CeA/PAG/BA`) driven **above the bearer's own
  running baseline**. **Bearer-pure** (the `_add_physical_percept` pattern), and **phasic** — an agent at rest is
  not displaying distress ("tonic cancels", the same logic as the teaching signal).
- Present it as the **existing** `vulnerable_other` trigger → `IN-VIS:biological_motion` → **SC-Pv → CeA**.
- The perceiver's response emerges from **its own** circuits (`felt_response` → CeA → LC → phasic NA). No
  parallel module, no coded other's-pain → my-response coefficient. **The display magnitude IS the bearer's
  evoked distress.**

Verified: bearer at rest displays **nothing** (`{}`); bearer in pain displays `vulnerable_other: 0.472`;
end-to-end, a real `_social_episode` with a distressed other raises the perceiver's NA teaching signal
**0.043 → 0.080** — the aversive teaching signal now depends on **what befell the other**. **No
emotional-contagion runaway** from the new cross-agent feedback (A's distress → B → A): arena viable
(peak 0.904), settled, tail-means 0.63–0.67.

## The faithful structural difference — already present, not built
| | CeA | LC | NA | VPL | S1 | S2-PPC |
|---|---|---|---|---|---|---|
| rest | 0.550 | 0.238 | 0.080 | 0.085 | 0.149 | 0.362 |
| **vicarious** (routed display 0.472) | 0.744 | 0.292 | **0.127** | 0.085 | 0.149 | 0.372 |
| **direct** pain (nociception 0.9) | 1.000 | 0.403 | **0.208** | **0.536** | **0.449** | **0.626** |

**Direct** engages CeA **and** the sensory-discriminative route (VPL/S1/S2). **Observed** distress engages the
**affective route only** — VPL/S1/S2 sit at rest. That is the established vicarious-pain dissociation, present
in the substrate without being built, and it is the whole of the vicarious/direct difference.

## The result, as-found (reported in the direction it came out)
**`vicarious < direct` EMERGED — uncoded** (NA 0.127 vs 0.208; CeA 0.744 vs 1.000) at the realistic routed
magnitude. The reduction arises from the **display transduction**: an agent's evoked distress display (0.472) is
sub-maximal relative to the noxious stimulus (0.9) that caused it. Nothing chose that number — it is the
bearer's own state.

**MEASUREMENT LIMIT (must travel with the result):** the **direct arm saturates** — CeA pins at 1.000 and NA
flattens at ~0.208 for **every** drive ≥ 0.4:

| drive | vicarious CeA / NA | direct CeA / NA |
|---|---|---|
| 0.2 | 0.628 / 0.099 | 0.956 / 0.181 |
| 0.4 | 0.712 / 0.119 | **1.000 / 0.209** |
| 0.6 | 0.806 / 0.142 | **1.000 / 0.208** |
| 0.8 | 0.913 / 0.169 | **1.000 / 0.208** |
| 1.0 | 1.000 / 0.198 | **1.000 / 0.208** |

So the direct value is a **ceiling, not a measure**. The vicarious/direct **ratio cannot be quantified** at CeA's
current operating point (rests 0.550, ceiling 1.000); the observed difference is a **LOWER BOUND** (direct is
understated). The earlier "vicarious ≈ direct" reading (0.212 vs 0.208) was exactly this artifact — **both arms
pinned at ceiling** — **not evidence of equality**. Resolve with CeA's operating point.

## Deferred, with reasoning (not attempted, not assumed)
**Life-course accumulation** of the ~1e-4/exposure control-edge tuning. CeA rests ~0.46–0.55 against its
**ungrounded** 0.1 setpoint — ~7–9× the erosion pressure LC carried — with **all** its afferents unpinned,
including `SC-Pv→CeA` (the vicarious route's own input) and the innate nociception link. Unlike LC's, that
pressure is **self-limiting rather than unsatisfiable**: CeA's elevation is afferent-driven, so the homeostat
**can** reach its target by scaling those afferents down — which means CeA's threat responsiveness **drifts
downward over developmental time toward an ungrounded scaffold constant**, a developmental trajectory driven by
a placeholder. (LC's mismatch was visible because it could never settle; CeA's is invisible because it can.) So
accumulation would be **accumulation-against-erosion of unknown size — uninterpretable**. One ungrounded
operating point, two blocked measurements.

## §4 — recorded as structure, not a test
`AFFECTIVE_EMPATHY` contains `CeA`, which is LC's **sole** afferent. The throttle abolishes the vicarious NA
signal (0.212 → 0.000) **by construction** — the manipulation contains the mechanism. The **structural claim** is
real and stated: *the affective-empathy network and the aversive-teaching pathway share CeA, so hypofunction of
the former necessarily impairs the latter.* It is **not** an independent test and is not run as one.

**New standing check registered — construct validity, distinct from no-result-is-a-target:** before any
throttle/lesion-based claim, verify the manipulation set does not contain the mechanism's own nodes. *"No result
is a target" protects against aiming at an answer; it does not protect against an experiment that cannot answer.*
Throttle-set audit registered: `AFFECTIVE_EMPATHY` simultaneously manipulates aversive teaching, aggression, and
the threat hub — and it is the CU study's primary instrument.

**Full suite green (535 tests, 0 failures).**
