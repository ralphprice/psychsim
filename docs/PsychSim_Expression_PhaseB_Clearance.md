# Phase B — CLEARED (`e345788`), with ONE item left before Phase C

**Verified against `origin/main`:** 89 circuits / 223 edges. `vlPAG`→`NRA` (strong), `dPAG`→`NRA` (strong,
lump disclosed in-source), `PAG-PANIC`→`NRA` (moderate), `NRA`→`NuAmb-vocal` (strong), `dACC`→`NuFac`
(moderate). All `basis: anatomy`, all receptor-signed AMPA, **every one carrying its citation and its
reasoning in-source.** `NRA` correctly `motor_effector`, `structural_element: None` (a driven relay, not a
gate — right call). Suite 539/0.

**The emotional motor system is efferent for the first time.** The in-seed *"infant-cry/protest output"*
outputs.

**And the golden not shifting is elegant evidence, not an absence of one** — a regression pin used to *prove*
the limb is purely efferent and nothing leaks back. That is the right instinct: the pin earned its keep by
staying still.

---

## Two notes on my own probes

**My challenge to the `moderate` band came back clean.** I went looking for whether "PAG-PANIC is our
dorsomedial column" was cited or inferred — and the source says it, verbatim: *"SCAFFOLD band; the column
mapping is disclosed, not measured."* **Third time this arc I have raised a flag that the record had already
answered.** The pattern is consistent and worth my noticing: **this seed discloses more than my queries
assume.**

**And your identification closes a gap I registered — I was wrong.** I wrote *"the dorsomedial column is
unrepresented entirely — register that too."* If `PAG-PANIC` is the dm column, then
**`vlPAG` (vl) + `dPAG` (dl+l) + `PAG-PANIC` (dm) = all four columns. The scheme is complete.**
**My registration is WITHDRAWN.**
⚠️ **But flag the identification itself for the audit, precisely because it is tidy:** `PAG-PANIC` is a
**Panksepp-era node** (`nodes: "periventricular/periaqueductal…"`) being mapped onto **Holstege's columnar
scheme** — a cross-framework reconciliation, inferred, disclosed. It may well be right. It is not measured,
and "it completes the scheme" is an aesthetic argument, not evidence.

---

## ⛔ Phase B is not finished — the facial route has NO limbic drive at all

You disclosed *"Morecraft's cited BA→M3 link is missing — the route is built, its cited afferent isn't."*
**The backward trace makes it worse than that reads:**

```
NuFac  <- dACC
dACC   <- aIns, DRN
aIns   <- mIns, rSMG-TPJ          <-- and that is the whole route
```

**Nothing from the amygdala reaches the face. Not BA, not CeA, not by any path.** The facial route is driven
end-to-end by **interoception and social cognition**. **An agent at `CeA` = 1.000 has no facial expression
whatsoever.**

**Contrast the vocal half, which is complete:**
`CeA → vlPAG-GABA ⊣ vlPAG → NRA → NuAmb-vocal` — limbic to effector, and the disinhibition architecture
carrying it. ✅

### Why this blocks Phase C rather than waiting for Phase D
**Phase C's entire job is to RE-MEASURE the display** — including re-deriving `vicarious < direct`, our clean
uncoded result. **Re-deriving it off an instrument whose facial half is inert for distress would give a
number that must change again the moment `BA→dACC` lands.**

**And that is precisely the failure we already made once.** The punishment read-out was **validated while LC
was dead** — and could never have been tone-invariant *by construction*. **Validating the display while the
facial route is dead is the same mistake with a different corpse.** Principle 6's extension applies: no
characterisation from a partial assembly.

It also matters for what comes after: **the psychopathy expression signature is largely FACIAL** — reduced
mimicry, shallow affect — and Phase D's dual pathway is `M1-face → NuFac` (volitional) against
`dACC → NuFac` (emotional). **If dACC carries no emotional drive, the "emotional" route is not emotional and
the dissociation is untestable.**

### Build
**`BA → dACC`** — Morecraft's cited link (the amygdala → M3 → facial nucleus route). **Disclose the lump
exactly as you did for `dPAG`:** `dACC` conflates the cognitive ACC with the midcingulate motor areas, so the
edge attributes to the whole node what M3 carries. **Then re-run, and report whether the face moves under
distress.**

**REGISTER — the third split candidate:** `dACC`'s cognitive/motor lump meets the §9 grain test (conflict
monitoring and a premotor face area do not develop or behave alike; they now have different outputs). Queue
behind `dPAG`'s dl/l split. **Neither now.**

---

## Carried
preBötC + respiratory rhythm (own pass; vocalisation un-breath-gated) · the inspiratory PAG route (unmodelled)
· `PAG-PANIC`'s own gate (**stands** — your over-generalisation correction accepted and recorded) · `dPAG`
dl/l split · `dACC` cognitive/motor split · the `PAG-PANIC` = dm identification (audit) · Phase D's
cortico-bulbar route **to the motoneurons, bypassing PAG/NRA** · the scan-vs-signature disjointness rule ·
`SC-Pv → dPAG` 0.50 Point-1 candidate · `dPAG-GABA`'s missing release · the reactivity/regulation fusion.
