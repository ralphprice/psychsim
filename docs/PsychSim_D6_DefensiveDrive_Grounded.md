# The defensive-drive pass — grounded (Half 2 answered + corrected; Half 1 is a lump diagnostic)

Both halves worked, and both moved off where the ruling put them. **Half 2's answer is (b), and it corrects
the "missing release" hypothesis — including a methodological error of mine the sweep caught.** Half 1 is the
tenth and eleventh lumps, keystone-bound — diagnosed, not built.

## Half 2 — the escape gate is SURMOUNTABLE. Answer: **(b), load-bearing bands. Not (a).**
The sensitivity sweep the ruling asked for — *"is there ANY setting at which a salient SC drive crosses?"*

**⚠️ My first sweep was invalid, and it is worth recording why:** I drove `IN-VIS:looming` — **which does not
reach `SC-Pv`** (its only visual channels are `face_like` and `biological_motion`). `SC-Pv` stayed at 0.130 and
nothing crossed, and I nearly reported "never crosses → release missing." **That is the exact error the whole
discipline exists to catch: a null result from a manipulation that never engaged the mechanism.** Caught it by
checking the source's channels.

**Corrected — drive `SC-Pv` directly and sweep:**
| SC-Pv | 0.30 | 0.50 | 0.584 | **0.70** | 0.85 | 0.90 |
|---|---|---|---|---|---|---|
| dPAG (current bands) | 0.000 | 0.041 | 0.073 | **0.121 ✓** | 0.194 ✓ | 0.203 ✓ |

**A salient SC drive crosses the gate at ~0.7, at the current bands.** So **(b): the bands are load-bearing,
and the gate is not insurmountable** — the "missing release" (a) is *not* what blocks escape. The relational
grounding confirms the bands are directionally right (**dense inhibition, sparse excitation** → gate strong
relative to drive), and principle 2 holds: the arbitrariness is in a threshold the bands set, not a value.

### ★ So the proximal blocker is the INPUT, not the release
- **`biological_motion` drives `SC-Pv` to 0.584 — sub-threshold (dPAG 0.073).** That is *correct*: you do not
  flee from *watching* distress. So the vicarious visual route via `dPAG` is correctly closed for observation
  by the threshold — **not by a missing release** (this refines S37 once more: `dPAG` is escape-gated, and
  observation is correctly sub-escape).
- **`IN-VIS:looming` — the canonical imminent-threat escape trigger — does not reach `SC-Pv` at all.** So no
  cue in the model drives `SC-Pv` above the escape threshold. **The escape-TRIGGER INPUT (`looming → SC-Pv`) is
  the real gap** — the model has the gate and the relay but not the trigger.

**On the release itself (Stempel's VGAT⁺ dip):** it is a real mechanism, but the paper does **not** name its
source and offers the forebrain-disinhibition sentence as an *analogy* to the vlPAG/CeA case — **so it stays
`UNRESOLVED`, not guessed** (the ruling's own instruction). It would *lower* the threshold; it is not the
proximal blocker. **Register: (i) `looming → SC-Pv` missing (the escape trigger); (ii) `dPAG-GABA`'s release
source unresolved — do not invent it.**

## Half 1 — `MeA → VMH`: the fallback is backwards, and it is the TENTH + ELEVENTH lumps
Confirmed and grounded: **`MeA → VMH` should be glutamatergic (+)** — MePV → VMHdm (our `VMH`, the
predator-defence node) is glutamatergic; the fallback picked GABA and killed the drive. **But it is one edge of
two lumped nodes, and its sibling edge is keystone-bound — so this is a diagnostic, not a build.**

| lump | subnucleus / part | transmitter | target | function |
|---|---|---|---|---|
| **`MeA` (10th)** | MePV | **glutamatergic** | VMHdm = our `VMH` | predator defence |
| | MePD | **GABAergic** | VMHvl = our `VMHvl` | reproduction / aggression |
| **`VMH` (11th)** | core | glutamatergic | (projects out) | the defensive drive |
| | shell | GABAergic | inhibits the core | local gate |

**Two consequences, both keystone-relevant:**
1. **`MeA → VMH`** (our predator-defence node): should be **glutamatergic (+)** — the grounded fix for
   `vlPAG`'s missing drive. Available.
2. **`MeA → VMHvl`** (currently **AMPA**, and it feeds the aggression keystone): probably **wrong**. MePD → VMH
   is GABAergic, and VMH's shell is GABAergic and inhibits the core — so the aggression route is very likely
   **`MePD (GABA) → VMH shell (GABA) → disinhibits the core`**, the same disinhibition architecture as
   `CeA → vlPAG-GABA`. **Our AMPA gives the right OUTCOME by the wrong MECHANISM — the v9 keystone-flip story
   exactly, on a keystone edge.**

**RULING obeyed: diagnose, do not build.** Fixing one sign inside a lump is how we got the `CeA→PAG` artifact.
The `MeA`/`VMH` split + both edges come as **one** pass, keystone-bound, surfaced first. **And Q2 (what drives
`vlPAG` once disinhibited) waits — my counterfactual's VMH=0.116 was measured on the wrong (lumped) node; re-run
after the grain is settled.**

## 🎁 Confirmed in passing
`AUD-brainstem → SC-Pv` is correct and load-bearing (the SC receives *visual and auditory* threats) — so
**Phase C's injection of the cry at `A1-belt` bypasses `SC-Pv` too.** The injection-depth finding is now doubly
confirmed: the cry reaches neither `SC-Pv` nor the IC as wired.

## Order (unchanged): `MeA`/`VMH` grain (both edges, keystone-bound, surface first) → then `vlPAG`'s drive as
its own question. Half 2 is answered: (b) + the `looming → SC-Pv` input gap + the release unresolved. **Door 1
`UNRESOLVED`; #2 open on doors 2 and 3. Nothing built.**
