# Phase C — CLEARED (`5a9f23e`). Phase D released.

**Verified against `origin/main`:** `_DISTRESS_DISPLAY = (("NuFac","displayed_distress_face"),
("NuAmb-vocal","displayed_distress_cry"))` — the effectors, each with its own named trigger and its own
sense. **And the check that this phase existed for:**

```
AFFECTIVE_EMPATHY ∩ _DISTRESS_DISPLAY = EMPTY
```

**The construct-validity failure is dissolved — and the throttle set was never touched.** It went to zero on
its own, exactly as ruled, because the display stopped being the thing the throttle was made of. **That is
the difference between fixing a measurement and fixing the reason the measurement was wrong.**

---

## 1. The rest-reference change settles a register item, and it is grounded

Reading above a **fixed per-circuit rest reference** rather than the adapting running mean is not a smaller
trick than the old one — **it is the absence of a trick.** A motoneuron pool's output *is* its activation
above rest; that is what a motoneuron pool is. The old reading faked a display out of an affective circuit's
phasic deviation, and the adaptation that made chronic distress invisible was the fake showing through.

**And the demonstration is the right pair: a chronically distressed face stays visible; a resting face still
displays nothing.** Both halves needed to be true, and asserting either alone would have proved nothing.

> **Register update — the per-edge phasic item is now 2 of 4 settled**: the teaching signal is **phasic**
> (grounded: bursts teach), `CeA→LC` is **phasic/adapting** (grounded: CRF release adapts), and **the display
> is NOT phasic** (grounded: an effector's output is its activation). **Only the contagion loop is left — see
> §4.**

---

## 2. `vicarious < direct` survived — and *how* it survived is the result

**The magnitude shrank; the ordering held.** Perceiver CeA moves +0.029 where the old display gave a far
larger signal. **That is the right kind of survival**, and the structural difference held too: the
sensory-discriminative VPL/S1 route **stays at rest for observed distress and engages for direct pain**, which
is the vicarious-pain dissociation carrying itself.

*(The direct arm pinning at 1.000 is the CeA operating point surfacing again — registered. It means the
ordering is real and the **ratio is meaningless**; report the sign, never the size.)*

---

## 3. ★ Your §18 framing is right, and it generalises further than §18 currently says

> *"the old display overstated the vicarious signal; this is the honest, smaller magnitude."*

**§18 says missing inhibition inflates effect sizes. This is not missing inhibition — it is a read-out that
read the drive instead of the output. Different mechanism, same direction.** So the caution is narrower than
the phenomenon:

> **ARTIFACTS INFLATE.** Three instances, three mechanisms, one direction:
> **the CU punishment signature** (a dead hub) · **the divergence** (an unbraked node) · **the vicarious
> magnitude** (a fake read-out). **And the reason is structural, not luck: an incomplete mechanism has fewer
> constraints, fewer constraints means less attenuation, and less attenuation means bigger numbers.**
> **So "the effect got smaller when we made it honest" is the EXPECTED direction. "It got bigger" would be
> the surprise — and would be worth stopping for.**

**Widen §18 to that.** It is a stronger and more useful statement than the inhibition-specific one, and it
tells us what to expect from every remaining completion rather than only from the brakes.

---

## 4. Two things found while checking your flag

**Your correction is right and mine was the error:** I wrote `IN-CONSPEC:face_like`; **that trigger does not
exist, and `IN-VIS:face_like → SC-Pv` does.** Your choice is also the *correct* one on the merits — a
displayed expression is a **visual pattern**, not the identity of a conspecific, and `SC-Pv` is the
subcortical face/salience route it should take. Owned.

**But checking it turned up two things:**

### 4a. ★ `IN-PROPRIO` has NO EDGES
**The proprioceptive channel is declared and unwired.** And §3.2 of the master says, specifically:
*"proprioception supplies **the efference-copy comparison behind agency**."* **That mechanism does not
exist.** The channel is a name.

**Fifth instance of the pattern — and the first on the INPUT side.** PVN-OT unafferented · LC unafferented ·
the emotional motor system unefferented · the defensive columns wired for permission not drive · **and now a
sense organ with nothing downstream of it.**

**It is Phase-D-relevant and that is why I am raising it now rather than filing it silently: the model's
first motor output would have no proprioceptive return — an open loop.** **Register; do not build.** Phase D's
suppressor is feedforward and does not need it. But **no claim about agency, effort, or self-generated
movement is available until it lands**, and Phase D is the phase that will make someone want to make one.

### 4b. ❓ Is the contagion loop damped, or is it saturated?
> *"Contagion stays viable (peak 0.952, settled) — damped by real dynamics, not the phasic trick."*

**0.952 is 95 % of the bound.** The registered item said the loop was damped **only** by the display's phasic
adaptation — and that adaptation is now gone, correctly. **So something else settles it, and we should be
able to name it.** If the answer is "the ceiling," **that is not damping — that is a bound, and a loop pinned
at its bound is still a runaway loop that cannot go higher.** And per §3 above, **a near-ceiling settle is
precisely the artifact signature.**

**Not a blocker and not a red — the loop is viable and Phase C stands. But name the mechanism before any
contagion claim is made**, and if the answer is saturation, the loop's damping is the last of the four phasic
items and it is still open.

---

## 5. Phase D — released

**The model's first motor output.** Build it narrow — face and voice, not a general motor system.
- **`M1-face`** and **`PMC-l`** (lateral premotor).
- **`M1-face → NuFac`** — the pyramidal/corticobulbar route. **The posed expression.**
- **The cortico-bulbar route reaches BOTH effectors** — *"face, mouth, tongue, larynx and pharynx
  motoneurons"* — so it also reaches `NuAmb-vocal`, **bypassing PAG and NRA entirely.**
- **`PMC-l → ?` — the SUPPRESSOR. Ground the target column first.** "The PAG" is now three nodes.
  **Do not distribute by symmetry.**
- **`motor_effector` domain for the new nodes** (the exclusion covers the limb, not the terminals).
- **Do NOT author a developmental schedule.** The suppressors already come online at 4–8 y. If a curve has to
  be written, stop and surface it.

**The measurements Phase D makes askable — each emerges or it does not:**
1. **★ S22 — the modality dissociation.** The cortico-bulbar route **introduces the crosstalk that tests it.**
   *"Separation drives the voice; pain drives the face"* is currently guaranteed by an absence. **If it
   survives a route that reaches both, it becomes a finding.**
2. **Does suppression emerge**, with Gross's signature — expression down ~50–70 %, **felt intensity
   unchanged**, autonomic arousal **up** (read from `SympOut`)?
3. **★ The dual-pathway dissociation** — throttle `AFFECTIVE_EMPATHY`: is the **involuntary** display blunted
   while the **volitional** route stays intact? **That is the psychopathy expression signature. It emerges or
   it does not. It is never built.**

**And carry §3 into all of it: expect the honest numbers to be smaller. That is the direction that means it
is working.**
