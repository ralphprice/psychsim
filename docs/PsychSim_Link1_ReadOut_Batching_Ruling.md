# Batching link-1 with the read-out re-derivation — RULING. **Batch the GATE, not the MEASUREMENT.**

**The performance work is clean and the fourth vacuous guard was handled exactly right. The batching question
is a real one, and it splits: these two changes DO interact — but only through one link, and the per-link
structure of the mirror measurement gives a clean resolution that saves the gate without losing attribution.**

---

## 1. Performance work — the discipline held

- **Memoisation: 22% on the develop path, proven byte-identical** (hash `87d1c908…` before/after). Not a
  claimed neutrality — a demonstrated one.
- **Shared fixture: 4 developments → 2, neutrality confirmed** by construction and by the reading tests passing
  unchanged.
- **Every speed claim measured, not speculated.** ✓

**And the fourth vacuous guard handled correctly:** measured 0.674 vs 0.653 = noise, ambiguous direction, **no
defensible assertion exists yet — so it was suspended with a resolution condition rather than proxied into a
green.** That is refusing to manufacture a second near-vacuous guard while fixing the first, which is the
harder and correct call. **The test-integrity sweep the audit called for, arriving organically — four instances
now, same shape each time.**

## 2. ★ The batching question — they DO interact, through exactly one link

**My batching principle: items touching non-interacting parts of the tree can share a gate; items whose effects
would mask each other cannot. Applying it:**

- **Link-1 changes what acts are PRODUCED** (provocation becomes perceivable → the substrate may aggress) —
  the MECHANISM.
- **The read-out re-derivation changes what the profile READS** (live anatomy: CEm output populations, VMHvl;
  double-gain removed) — the INSTRUMENT.

> **These interact through LINK 5 — the profile divergence, which is the mirror's actual claim. If both land
> before the mirror is re-measured, a change in the measured defensive direction cannot be attributed: is it
> link-1 producing aggression (the mechanism), or the re-derived read-out sets reading the defensive output
> differently (the instrument)? That is PRECISELY the confound the entire audit was about — a measurement layer
> changing at the same time as the thing it measures. And this session has shown repeatedly that instrument
> changes move effects.**

**So they must not be measured together. But that does NOT mean two gates.**

## 3. RULING — separate the MEASUREMENT, batch the GATE

**The per-link structure gives the clean resolution: links 1–4 do not depend on the read-out sets (acts
produced, ties strained, cues presented — all measured on exchange dynamics and the cue channel); only link 5
does. And a measurement does not require a gate.** So:

1. **Build link-1** (Routes 1 and 2 — `separation`→provocation, negative `displayed_affect`→provocation,
   present-not-assign).
2. **Measure links 1–4 IMMEDIATELY** — no gate needed, it is a measurement. **This verifies the MECHANISM on
   the pre-change instrument, cleanly attributed to link-1 alone.** Does provocation now enter the exchange?
   Do aggressive acts emerge? Do ties strain? Does the wary cue present? **Include the sustained-provocation
   measurement here** (the Tremblay caution — does repeated rejection escalate, before characterising the
   single-seed absorption as a defect). **This phase is about act production, not the read-out, so it belongs
   entirely pre-re-derivation.**
3. **Then build the read-out re-derivation** (+ double-gain removal).
4. **Gate BOTH together — one cycle.** Regression clearance can cover both changes; if the gate surfaces
   something ambiguous, **bisecting is cheap relative to a full extra 7h45m gate**, and this session's record is
   that gate failures have traced cleanly to diagnosed causes.
5. **Then measure link 5 — the profile divergence — on the CORRECTED instrument.** That is the number that
   goes in the thesis: the defensive-direction magnitude, measured on read-out sets that point at live anatomy
   with the double-gain removed.

> **This preserves attribution exactly where it matters (the measurement) while saving the gate cycle. The
> mechanism is verified against link-1 alone; the claim is measured on the corrected instrument. Nothing is
> conflated, and one gate is spent instead of two.**

**A further reason the read-out re-derivation should FOLLOW rather than precede:** it re-enables the climate
test just suspended (whose resolution condition points at it), so it lands with that resolution ready — and it
is better done after link-1's mechanism is confirmed, so the re-derived sets are validated against a defensive
output that is actually being driven.

---

## 4. Handoff

**Build link-1 (Routes 1 and 2) now. Measure links 1–4 immediately — no gate — including the
sustained-provocation escalation measurement (Tremblay caution: measure before characterising the single-seed
absorption). Report the mechanism result.**

**Then build the read-out re-derivation + double-gain removal, gate both together in ONE cycle, and measure
link 5 (the profile divergence) on the corrected instrument — the thesis number.**

**Then the remaining gate-requiring queue:** the DA second front, the adult-plasticity floor grounding, the
deliberate golden regeneration (after the read-out work, with the note), and the record-integrity pass.

> **The performance work held the discipline — measured speed claims, proven neutrality, and a fourth vacuous
> guard refused rather than proxied. On batching: link-1 and the read-out re-derivation interact through link 5,
> the mirror's actual claim, and conflating a mechanism change with an instrument change is the exact confound
> the audit was about. But the per-link structure separates them cleanly — links 1–4 are measured on exchange
> dynamics and need no gate, so build link-1, measure the mechanism immediately and alone, then build the
> re-derivation, gate both in one cycle, and measure the claim on the corrected instrument. Attribution
> preserved where it matters; one gate spent instead of two. Build link-1.**
