# `MeA`/`VMH` build depth — **RULING: (A).** With a better reason and one requirement.

---

## 1. (B) is not "more thorough." It is BLOCKED — on the input surface, and by the `PrH` precedent

**MePV's defining afferent is PREDATOR ODOR** — those cells *"increase their activity in response to **cat
odor**."* That is what makes MePV the defensive arm and MePD the reproductive one. **It is the whole basis of
the distinction.**

**And I checked: `MeA ← PIR, V-ventral`. That is its entire afferent set. There is no predator channel** —
`IN-OLF → OB` is generic, and the `IN-CONSPEC` channels are `attractive_face`, `formidability_cue`,
`kin_signature`.

> **So splitting `MeA` would produce `MePV` and `MePD` with IDENTICAL afferents — two nodes that cannot be
> driven apart.** That is the **`PrH` ruling verbatim: do not build a node whose defining afferent is an
> unbuilt system.** It would be the failure mode we are fixing, committed inside the fix for it — **for the
> third time on this branch.**

**→ REGISTER a channel gap: predator odor.** Joining `PB-LOOMING`, `PB-STARTLE`, `PR-SALT`,
`PR-HOMEOSTATIC`, `IN-PROPRIO`. **Your input-surface audit predicted this one before we needed it — which is
what a good audit does.** **And it names (B)'s unblock condition precisely: the split becomes buildable when
the input surface can tell a predator from a conspecific, not before.**

**So (A). Your lean is right; the reason is that (B) is unbuildable, not merely large.**

---

## 2. But state the lump's cost exactly — because (A) makes it WORK without RESOLVING it

**The receptor convention gives sign per EDGE, not per node.** So `MeA` can be glutamatergic to `VMH` **and**
GABAergic to `VMHvl-GABA` simultaneously, and both edges will be right.

> **That makes the lump functional without dissolving it: MePV and MePD still fire TOGETHER, because they are
> one activation. A predator cue and a conspecific cue drive BOTH routes. That is the cost, it is real, and
> it stays until the input surface can separate them.** **Write it into the seed as the disclosure, not the
> commit message.**

---

## 3. ⚠️ Surface the sign flip's consequence BEFORE you build it

- **Before:** `MeA → VMH` = **−1** → MeA **suppresses** the defensive column → `vlPAG` dead.
- **After:** `MeA → VMH` = **+1** → **MeA DRIVES the defensive column** — and **`MeA` fires on any olfactory
  or conspecific cue** (`PIR`, `V-ventral`).

**So a neutral conspecific encounter may now produce freezing.** That is not a reason to hesitate — it is the
grounded value — **but it is a reason to build the guard in the same pass.**

> **★ THE FREEZING FLOOR. This is the first time `vlPAG` can fire, which makes it the first time freezing can
> be tested at all. It needs its own neutral floor — the exact analogue of v9's aggression floor: NO THREAT
> → NO FREEZING.** **Build it WITH the fix, not after.** And per the v9 lesson, which cost us this entire
> branch: **if it passes, ask what is holding it.** A floor that has never existed is not a floor that has
> held.

---

## 4. The gate — `VMHvl-GABA`

- **Default to the RECURRENT-E-I class: scaffold baseline, ground the LOOP** — unless the electrophysiology
  says the VMH shell is tonic. **That is the gate taxonomy's default, and forcing a rate would invent a
  pacemaker.** `vlPAG-GABA` is the precedent and it is the same structure.
- `structural_element: true` (temperament-excluded, scan-lesionable) · cite the shell/core anatomy · **and if
  it has a structural twin, inherit its bands byte-for-byte and say so.**

## 5. And your keystone discipline is right — recorded

> *"require it to hold on the mechanism, not just the number"*

**That is the v9 lesson applied unprompted, on the edge where it was learned.** `MeA → VMHvl` at `w0 = 0.10`
against a keystone riding on `provocation → VMHvl` and `CeA → effectors` at 0.70 **is a small risk arithmetically
and exactly the kind that has surprised us before.** Re-run it and interrogate what holds it.

---

## Build
`MeA → VMH` sign re-grounded (glutamatergic, MePV, cat-odor cited) · `VMHvl-GABA` shell interneuron ·
`MeA → VMHvl-GABA` (GABAergic, MePD) replacing the AMPA-direct edge · **the freezing floor** · the lump's cost
disclosed in-seed · keystone re-run **on the mechanism**.

**Then:** `IN-AUD → COCH` (own pass, own clearance — startle fires for the first time) → `vlPAG`'s drive
re-run on the settled node → door 1's auditory half → the channel gaps.

**Door 1: visual `TRUE BY ANATOMY` (measured), auditory live. #2 open on doors 2 and 3.**
