# `test_history_shapes_behaviour` — RULING. **Restate (option 1), but it must ASSERT, not defer. And the
# pattern is now three-for-three — register the principle.**

**Your lean is right and consistent with how F4 and the divergence sentinel were ruled. One addition, because
without it we retire an assertion on the study's dev-social mechanism and replace it with nothing.**

---

## 1. Option (1) confirmed — and option (2) is correctly rejected

**Restate the test. Do NOT reconsider the routes.** Reverting grounded, ruled-to-keep anatomy to preserve a
one-act-out-of-162 assertion is backwards, and you named it correctly. The routes are grounded, they were
ruled, they affect both arms equally, and they are not the defect.

**The diagnosis is right:** the F2 relational effect at the aggregate-act level was ONE act out of 162 —
knife-edge from the start. The routes nudged the operating point and collapsed it. **The mechanism is fully
intact** (ties form and differentiate, 6 ties; the substrate-level F4 tests — byte-identical stream, weight
divergence — all passed in the gate). **Only the brittle discretized assertion broke.**

## 2. ★ The addition — the restatement must ASSERT a measured quantity, not defer to other tests

**The risk in a serial-restatement pattern is that the study's assertions get progressively weakened until
nothing can fail. The guard against that is simple and it applies here:**

> **The restated test must ASSERT something with a measured, adequate margin — not merely point at the mechanism
> tests that already pass. Otherwise we have retired a guard on the dev-social mechanism and added nothing.**

**So: measure first, then assert.** Establish the actual magnitude of the relational effect on a CONTINUOUS
quantity — the weight-divergence magnitude between arms, or the tie-affect differentiation spread — **and set
the assertion at a level that is robust (comfortably above noise), recording the measured value and the margin
in the test.** That keeps a real, falsifiable assertion on the study's dev-social mechanism, stated on the
quantity that actually carries the effect.

**If the measurement shows NO continuous quantity carries the effect at a robust margin, that is itself the
finding** — and it should be recorded as such (the relational effect on behaviour is real at the substrate level
but below robust detectability at the aggregate level), not papered over with a softer assertion.

## 3. Register the principle — three-for-three is a finding, not three coincidences

**F4's label divergence (bare argmax over a 0.0021 margin), the divergence sentinel (sign stability on a
noise-scale quantity), and now the aggregate act Counter (one act out of 162) are the same failure:**

> **A DISCRETIZED read-out of a continuous underlying quantity, asserted at a magnitude that was always
> noise-scale. The discretization (argmax label, sign, integer counter) throws away the graded information, and
> the assertion then tracks noise rather than the mechanism. The mechanism held in all three cases; only the
> discretization broke.**

**RULING — register as a standing principle: study-layer assertions belong on the CONTINUOUS quantity that
carries the effect (profile shift, weight divergence, tie differentiation, margin), not on a discretization of
it (label, sign, counter). Where a discretized assertion is genuinely wanted, it must carry a measured adequate
margin — the F4 margin-gate pattern — so it cannot pass or fail on noise.**

**And feed this into the test-integrity sweep as a FOURTH shape.** The sweep was scoped for vacuous guards
(tests that read the property they validate, or bound an effect that has vanished — three instances). **This is
the mirror image: brittle discretization assertions, which FAIL for the wrong reason where vacuous guards PASS
for the wrong reason. Both are tests whose green/red does not track the thing they name.** Same sweep, both
shapes.

## 4. The perf result is worth marking

**592 tests in 73.6 min against gate 3's 7h45m — 6.3×, validated at suite scale.** That materially changes the
economics of the remaining queue (DA second front, floor grounding, golden regeneration, the integrity sweeps),
and it was earned with byte-identical proofs rather than claimed. **Good work — it will pay for itself several
times over in what's left.**

---

## 5. Proceed

1. **Measure** the relational effect's magnitude on a continuous quantity (weight divergence between arms, tie
   affect differentiation) — establish what is robustly there.
2. **Restate `test_history_shapes_behaviour`** to assert that quantity with an adequate measured margin,
   recording the value and the finding (the aggregate-act assertion was knife-edge from the start and is
   retired as brittle). **If nothing carries it robustly, record THAT as the finding.**
3. **Register the standing principle** (§3) and add the brittle-discretization shape to the test-integrity
   sweep.
4. **Close the gate cycle**, then the remaining queue: the DA second front, the adult-plasticity floor
   grounding, the deliberate golden regeneration (with its note), and the record/test-integrity sweeps.

> **Restate, don't revert — the routes are grounded and the one-act assertion was noise from the start. But the
> restatement must assert a measured continuous quantity with an adequate margin, or we have retired a guard
> and replaced it with nothing; and if no continuous quantity carries the effect robustly, that null is the
> finding and gets recorded. Three instances of the same shape is a principle, not a coincidence: study-layer
> assertions go on the graded quantity, never on a discretization of it, unless the discretization carries a
> measured margin. Add the shape to the integrity sweep. The mechanism held all three times — it is the
> instruments that keep proving brittle, which is the audit's finding continuing to be right.**
