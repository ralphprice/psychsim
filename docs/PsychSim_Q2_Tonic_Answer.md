# Q2 — the tonic question: **ANSWERED. Not tonic. Q2 is an EDGE.**

**And the experiment is in the paper we already rely on — the same one that grounded `dPAG-GABA`'s 0.19.**

---

## 1. The measurement — it is the exact test, and it is a null

**Stempel & Evans ran precisely this, in the dPAG:**

> **VGluT2⁺ dPAG neurons were NOT spontaneously active — 92.8 % (39 of 42 cells) below 0.04 Hz; mean across
> all neurons 0.11 ± 0.068 Hz.**
> **And the decisive half: AFTER BLOCK OF INHIBITORY SYNAPTIC TRANSMISSION (50 µM picrotoxin) — 0.039 ±
> 0.033 Hz. P = 0.85. NO CHANGE.**

**Remove the gate entirely and the glutamatergic neurons still do not fire.**

> **"Disinhibition only permits; something must drive" is CONFIRMED — by direct experiment, not inference.**
> The contrast is the whole point of that figure: **VGAT⁺ 4.7 Hz spontaneous, unchanged by blockers, CV₂ =
> 0.33 (cell-intrinsic). VGluT2⁺ ~0.1 Hz, unchanged by picrotoxin.** One population is a pacemaker; the other
> is driven. **We already grounded `dPAG-GABA`'s baseline from the first half of that figure. The second half
> answers Q2.**

**And it disposes of the phrase that raised the question.** Vaughan & Christie's *"leading to activation of
the glutamatergic projections"* is a **circuit description from the analgesia literature** — it means the
output rises when the brake comes off a **driven** population, not that the cells self-start. **An unmeasured
phrase does not outweigh a measured null.**

### ⚠️ Disclose the extrapolation — it is dPAG, not vlPAG
**The measurement is in the dorsal column. `vlPAG` was not the recorded tissue.** It is the closest direct
evidence, the same technique, the same lab, the exact experiment — **but it is an extrapolation across
columns, and this branch has already burned us twice on treating columns as interchangeable.** **Record it as
`ANSWERED BY ANALOGY (dPAG), vlPAG-specific measurement UNRESOLVED`** — and if a vlPAG-specific recording
ever contradicts it, that is a revision, not a surprise.

> **RULING: keep `vlPAG`'s 0.05/0.1 scaffold. Q2 is a grounded AFFERENT, not a grounded baseline. Find the
> driver.**

---

## 2. ★ And the search changed the node: `vlPAG` is a FOUR-transmitter lump — the twelfth

> **"The vlPAG is comprised of a diverse population of neurons including glutamatergic, GABAergic,
> **serotonergic and dopaminergic** cells."**
> And there is a whole paper titled *"Ionic currents influencing spontaneous firing and **pacemaker
> frequency** in **dopamine** neurons of the ventrolateral periaqueductal gray and dorsal raphe nucleus
> (vlPAG/DRN)."*

**So the vlPAG DOES contain autorhythmic pacemaker cells — the DOPAMINERGIC ones. Not the glutamatergic
ones.** **The tonic population that made your question plausible is real, and it is a different cell type.**
That is the target-cell resolution one more time, and it is the reason the question looked open.

### And it gives the `DRN` grain note a mechanism rather than an observation
`DRN` and `vlPAG` are not merely adjacent. **The model's `vlPAG` node CONTAINS the serotonergic and
dopaminergic cells that ARE the DR** — *"the DR is embedded in the vlPAG… vlPAG-DR is often regarded as the
same area."* **So `DRN → vlPAG` is an edge from a node to a node that contains it.** Not a sign error; a
grain error. **Register `vlPAG` (glut / GABA / 5-HT / DA) on the census — and note it is the only lump we
have found where the two nodes are the same tissue.**

---

## 3. Step 2 — `vlPAG-GABA`'s afferent set. That is the work.

**The selector integrates multiple inputs; ours has two.** Enumerate the cited set as we did for `LC` and
`PGi`; **record INCOMPLETE with the remainder named** (S38).

**Your driver triage stands and it is correct:**
- **`mPFC` top-down — buildable.** `dmPFC`/`vmPFC` exist and are afferented; **it passes the `PrH`/`MePV`
  test.** Ground the specific projection before routing it (which subdivision, which target cell — and note
  a glutamatergic PFC source reaching a GABAergic selector *suppresses* freezing, while reaching `vlPAG`-glut
  directly *drives* it; **the target cell decides the sign, again**).
- **`PMd` — absent. Block-test it first.** ✅ Exactly right, and it is the third time that test has earned its
  keep.
- **The opioid system — its own pass.** ✅ **Four circuits name opioids in their prose and none has an opioid
  afferent** — that is the transmitter-in-the-vocabulary-with-no-connectome shape, and `PAG-PANIC`'s soothing
  arm wants the same system. **Two registers, one circuit. Not a Q2 footnote.**

**`DRN → vlPAG` and `VMH → vlPAG` stay untouched. The freezing floor stays red, and it is now red for a
reason we can state in one line: `vlPAG`'s selector has two of its afferents and its driver is unfound.**
