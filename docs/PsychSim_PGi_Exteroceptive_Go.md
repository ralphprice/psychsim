# `PGi`'s exteroceptive arm — **GO on the diagnostic.** One flag.

**Verified `47053b0`:** 94 circuits / 235 edges. The earned negative un-xfailed **with the whole argument
written into the test** — §18's directionality, the arc as a measured law, the a-fortiori reading. The
reasoning now travels with the test instead of living in a ruling nobody re-reads. `PGi`
(`interoception_autonomic`) ← `PBN`, `NTS`, `IN-SOMATO:nociception` → `LC`. Clean.

---

## ⚠️ The flag: `SC-Pv → PGi` is a candidate we want to be true

I checked it. **`SC-Pv` ← `RET`, `IN-VIS:face_like`, `IN-VIS:biological_motion`, `AUD-brainstem`, `FEF`.**
So the candidate is:

1. **Already built** — no new circuit needed.
2. **Already afferented** — it passes S35's recursion test, unlike `PrH`.
3. **Carrying `IN-VIS:biological_motion` — which IS the vicarious distress cue.**
4. **So it would dissolve the entailment in exactly the context we need it to.**

**Four for four.** And the fifth:

> **Its grounding is a FUNCTION description, not a cited projection.** *"PGi integrates exteroceptive and
> interoceptive sensory inputs"* names **what PGi does** — it does not name **which exteroceptive source
> reaches it.** **Structure is the one thing this model claims is grounded, and a functional description is
> not a citation for an edge.** This is the shape of error we have caught four times this arc, and it is
> more dangerous here than usual **because the answer we want is available and would work.**

**"No result is a target — both ways."** The forbidden move is not only aiming at a wanted result; **it is
accepting a wanted-looking result that an artifact might have produced.** A convenient candidate routed on a
functional inference is exactly that.

---

## The diagnostic's question — ask the one that produced everything in this arc

**Not** *"what could carry exteroceptive input to `PGi`?"* — that question has a convenient answer waiting.

**Ask:** **"What does the literature say `PGi`'s afferent set IS?"**

**And the paper exists, and it is literally the afferent study:**
> **Van Bockstaele, Akaoka & Aston-Jones 1993 — "Brainstem afferents to the rostral (juxtafacial) nucleus
> paragigantocellularis: integration of exteroceptive and interoceptive sensory inputs in the ventral
> tegmentum."**

**That is the LC-afferent question one synapse down — and the LC-afferent question is what produced this
entire finding.** Note its title says **brainstem** afferents; `SC-Pv` is midbrain tectum. **That may be fine
(the tecto-reticular projection is real) or it may be that PGi's exteroceptive arm is not collicular at all.
Find out; do not infer.**

**If `SC-Pv → PGi` is in the cited set: excellent, and it is the best possible outcome. If it is not: do not
route it there because it would work.**

---

## And apply S35 to each candidate, as you did to `PGi` itself
For every candidate afferent: **is IT afferented, or does it move the incompleteness one synapse further?**
That is the `PrH` ruling, and it is why `PrH` is registered rather than built. **`SC-Pv` passes it — which is
a reason to check it carefully, not a reason to accept it.**

## The success criterion is S35's, not anatomy's
**Door 1 closes when the entailment dissolves UNDER VICARIOUS DISTRESS — the context the CU study's claim
lives in.** Not when `PGi` looks complete. **64 % is the number to move; 37 % under direct pain is the number
that would look like a resolution.**

**Diagnose, surface, hold. #2 stays open on doors 2 and 3 regardless.**
