# Lump #13 CLOSED — the descent is complete. Verified. **Gate it clean, then the harsh mirror.**

**I verified the close against the remote. It is real, and it is the honest kind of close — the exit
measurement met by grounded anatomy, not by weakening a criterion. What just closed is the longest descent in
the project, and it deserves to be stated precisely.**

---

## 1. The close — verified against the remote

- **The CeL competing pair is complete:** `CEl-CRF` present, **mutually inhibitory with `CEl-SOM`** (both
  directions, GABA-A strong — the Fadok competitive circuit), **driving the active column via `dPAG-GABA`
  disinhibition** (the freeze-arm lesson applied — releasing a drive, not adding one). `CEl-PKCd`, `CEm-active`,
  `CEm-freeze` all present.
- **The refuted edge stays gone:** `CEl-SOM → CEm-active` is absent; SOM+'s efferents are the three grounded
  local targets (`CEl-PKCd`, `CEl`, `CEl-CRF`) — the mutual inhibition and the local brake, exactly the
  grounded role.
- **The freezing driver is in:** `dmPFC → vlPAG` (prelimbic-analogue, AMPA, moderate).
- **101 circuits, 265 connections, library regrown.**

**The exit measurement is met the honest way:** freezing and active defence are distinct selector states that
DO NOT trade — each context drives its own column and leaves the other at exactly zero. And the counterfactuals
prove CRF+ is load-bearing, not decorative:
- **Silencing CRF+ collapses `dPAG` under provocation** (0.0393 → 0.0000) — the flight column genuinely depends
  on it.
- **Silencing CRF+ lets SOM+ saturate under threat** (0.563 → 1.000) — the pair is genuinely competitive, and
  CRF+ is what keeps its rival off the ceiling.

**The selection EMERGES from drive balance. There is no scaffold coordinate it rests on.** The exit condition
the whole staging aimed at is satisfied.

## 2. What closed — the longest descent in the project

**CeA saturating → the coupling (freezing needs CeA high, aggression needs it low) → the reframe (the lump was
FLATTENING the column, not serving two functions) → the un-lumping (CEl/CEm) → the selector (SOM+/PKCδ+) → the
two blockers (vlPAG's missing driver, the refuted SOM+ edge) → the competing pair (the missing CRF+ flight
population).** Every step was forced by measurement and grounded in anatomy. The descent began because
relationships could only warm — the substrate couldn't produce an aggressive act — and it ends with the
aggression output expressible as an emergent selector state. **That is the whole arc, closed.**

## 3. The method held — three things worth marking

1. **The emergence test was rewritten to assert the boundary EXISTS and MOVES, not where it sits.** When CRF+
   landed, the SOM+/PKCδ+ crossover moved (threat 0.25 → 0.35). The tempting fix was to bump the coordinate;
   the build session rewrote the test to assert the property instead. **This is the failure-mode principle
   applied exactly: a test that pins a coordinate breaks on legitimate structural change and invites the
   fitting move the discipline forbids. The property was intact; only the coordinate changed.** The test now
   survives structural change and cannot be satisfied by fitting.
2. **The vlPAG lesson was checked BEFORE building the active column.** CRF+ is more active under threat (0.221)
   than provocation (0.115), yet dPAG fires only under provocation — because `dPAG` carries real excitatory
   afferents (SC-Pv looming, VMHvl attack) where `vlPAG` had only non-threat VMH. **That asymmetry is exactly
   why vlPAG needed PL excitation and this column didn't — disinhibition releases a drive only where a drive
   exists. Checked, grounded, and asserted as a test so it can't regress.**
3. **Both registrations are omitted-not-forced — the honest choice.** The CRF/SOM overlap (McCullough, Hartley)
   registered as a known simplification. And Fadok's CRF+ → VMH projection NOT built, because it targets VMHdm
   (predator defence) which the model lacks — **omitted rather than mapped onto the model's reproductive/social
   VMH or its VMHvl attack area.** Forcing a real projection onto the wrong target would have been the kind of
   convenient mis-grounding the whole project guards against. Omitting it and registering the gap is correct.

**Both prior honest negatives resolved by grounded anatomy, not by weakening a criterion. 15 selector tests,
all passing, no expected failures.**

## 4. RULING — gate now, then the harsh mirror

**Gate now.** `858a10a` and `3896e69` are un-gated, and the closed descent should be cleared clean. **Per the
batching principle: batch only what is ready to land in the same cycle.** The DA second front and the read-out
re-derivation are diagnoses/changes NOT yet made — they are not ready to batch. **So gate the closed descent
NOW on its own** (it is a substrate close that needs its own clean measurement anyway), and the DA second front
and read-out re-derivation get their own cycles when they are built (each changes dynamics and needs clean
measurement).

**Then — the harsh mirror.** This is the thing the entire descent was in service of, and it is NOW UNBLOCKED:
the aggression output is expressible. **The harsh mirror — the wary → defensive developmental direction, owed
since F1–F4 for the study's even-handedness — becomes available the moment the aggression output can be
produced, which is now.** Open it as the next real work after the gate clears:
- Run `run_life(relational=True)` with a harsh cohort (relationships that strain through hostile exchanges) and/
  or a harsh moral environment, and measure whether the classified developmental outcome diverges toward the
  defensive/CeA direction — the mirror of the warm → reward result.
- **Same honesty control:** the two arms differ only in relational content; the wary relationships must EMERGE
  from hostile mutual exchanges (now producible, because the aggression output is expressible), never be
  assigned.
- **AND re-measure it on the corrected instruments** — the age_window fix and the read-out re-derivation change
  what the classified outcome means, so the harsh mirror should be measured after those land (or measured now
  on the substrate/profile with the caveat that the classified-label read-out is still being corrected). **The
  honest form: demonstrate the wary → defensive direction on the substrate/profile (which the S18-corrected
  instruments will read faithfully), showing the mechanism produces BOTH developmental directions from the same
  machinery.**

---

## 5. Handoff

**Gate the closed descent now (858a10a, 3896e69 un-gated) — on its own, since the DA second front and read-out
re-derivation are not yet built to batch with it. The gate clears the longest descent in the project.**

**Then the queue, in priority: (1) the harsh mirror — now unblocked, the aggression output expressible, owed
since F1–F4 — measured on the substrate/profile (and re-measured after the read-out re-derivation corrects the
classified-label instrument); (2) the DA second front (the associative-specific erosion — R8 vs E-I damping);
(3) the read-out re-derivation over live anatomy + double-gain removal; (4) the adult-plasticity floor
grounding; (5) the record-integrity + test-integrity sweeps.**

> **Lump #13 is closed and verified: the CeL competing pair complete, the selection emergent from drive balance,
> the counterfactuals proving CRF+ load-bearing, freezing and active defence as distinct non-trading selector
> states. The longest descent in the project — from CeA's saturation through the coupling, the un-lumping, the
> selector, the blockers, to the competing pair — is done, and done the honest way: grounded anatomy resolving
> two standing negatives, a coordinate-pinning test rewritten to assert the property instead, the vlPAG lesson
> checked before building, the un-groundable projection omitted rather than forced. Gate it clean. Then open the
> harsh mirror — the aggression output is finally expressible, which is what this entire descent was for. The
> substrate can now produce both developmental directions from the same machinery, and the even-handedness the
> study's credibility needs is at last demonstrable.**
