# Stage A — RULING. **The measurement inverted our shared assumption. The coupling was SUPPRESSION, not
# competition. Scope correction authorized.**

**The measurement did what measurement has done all session: it refuted a story both roles were carrying. I
verified it against the code. The finding reframes what the whole un-lumping must achieve, and the scope
correction (CEm needs an excitatory afferent) is grounded. Here is the ruling.**

---

## 1. The reframe — verified, and it is the important result of Stage A

**Both of us carried the assumption that the coupling was "one activation serving two opposite functions"
(freezing needs CeA high, aggression needs CeA low). The measurement shows that was BACKWARDS:**

> **The saturated CeA lump was not DRIVING the defensive column — it was FLATTENING it. CeA is GABAergic;
> pinned at 1.000 it crushed all six targets to the floor (pre-split: vlPAG-GABA, HYPdm, PVN, RVLM all 0.000
> under threat). The coupling was never one activation serving two functions — it was one activation
> SUPPRESSING EVERYTHING. That is why freezing AND aggression both failed to be drivable: not because they
> competed for one CeA state, but because the saturated lump suppressed both outputs uniformly.**

**And Stage A produced the opposite error:** CEm, with zero external afferents, is silent (0.000 in all
conditions) — it delivers no inhibition, so the six targets rise on their other afferents, but CEm itself
drives nothing. **The lump crushed uniformly; Stage A releases entirely. Neither is right.** This is verified:
CEm's only afferents are `CEl-GABA` (strong, inhibitory) and `ITC` (inhibitory) — **CEm receives only
inhibition and cannot produce output.**

> **This is a genuine and important reframing, and it is the kind of thing only measurement surfaces: the
> defensive column's failure was a SUPPRESSION pathology (a GABAergic hub saturating and flattening its
> targets), not a SELECTION pathology (one activation forced to serve two modes). The un-lumping is still the
> right fix — but for a sharper reason: CEm must become a node that can DRIVE differentiated outputs under
> selection, replacing a lump that could only suppress.**

## 2. RULING (a) — the scope correction is authorized: `BA → CEm`, grounded, one edge becoming two

**CEm needs an excitatory afferent — the ruled "CEm takes zero external afferents" was anatomically wrong for
CEm, and the measurement proves it (a node with only inhibitory input cannot participate in a disinhibitory
motif; there is nothing to disinhibit).** The grounded remedy is verified:

- **`BA → CEm`** — BLA (basal amygdala) projects to BOTH central divisions; `BA → CEl` already exists and BA is
  glutamatergic (excitatory). **So `BA → CEl` becomes `BA → CEl` AND `BA → CEm` — exactly parallel to the ITC
  split already authorized: one edge becoming two, grounded in the BLA→CeM projection, not an invented drive.**
- **Over the registered alternatives:** `PBN → CEm` (real but CeL/CeC-dominant — the diagnosis correctly ranked
  it lower); `LA → CEm` (weak — LA reaches CeM largely via BA, so a direct edge would double-count). **`BA → CEm`
  is the grounded principal driver.**
- **This makes the disinhibitory motif WORK:** CEl inhibition (via CEl-GABA) now relieves a drive that is
  actually present (the BA excitation of CEm), rather than gating a silent node. **Without it, Stage C's
  selector would gate nothing.** Authorized.

## 3. RULING (b) — the reframe sharpens Stages B and C

**With the coupling understood as suppression-not-competition, the resolution is clearer:**
- **Stage B (differentiate CEm outputs):** CEm, now driven by BA, must have differentiable output populations
  (freezing-output → vlPAG-GABA; aggression-output → HYPdm) — so that the shared BA drive can be routed to one
  output or the other. **The differentiation is what lets one CEm drive freezing in one context and aggression
  in another, instead of a lump that could only suppress both.**
- **Stage C (the CEl SOM+/PKCδ+ selector):** the selector routes the drive — gating CEm's freezing-output
  population in a freezing context and its aggression-output population in a provocation context. **The
  selection is the mechanism that makes the shared drive produce ONE mode at a time.** And Stage C also delivers
  the ceiling relief (see below).
- **The exit measurement is unchanged but its meaning is sharper:** freezing and aggression drivable as
  distinct selector-gated CEm output states — which is now understood as "a driven CEm whose differentiated
  outputs are selected between," not "one activation resolving a competition." **The coupling resolves when CEm
  can drive either output under selection, rather than a lump suppressing both.**

## 4. RULING (c) — the ceiling did not relieve at Stage A; that is Stage C's, and it measured as predicted

**You flagged the ceiling relief as a prediction to measure, not an assumed benefit. It measured FALSE at Stage
A** — the saturation moved to CEl (15 afferents, no local brake, because the brake was the CeL→CeM edge that
became CEl-GABA→CEm and left). **This is not a failure — it is the prediction resolving, exactly as the
diagnosis framed it.** The ceiling relief is **Stage C's to deliver:** the SOM+/PKCδ+ selector provides CEl's
missing local inhibition (the mutual-inhibition microcircuit IS a local brake on CEl), which is what should
relieve CEl's saturation. **Register: CEl saturation is expected until Stage C adds the selector's local
inhibition; the ceiling is a Stage-C deliverable, confirmed by Stage A's measurement.**

## 5. RULING (d) — the `CeA → LC` severance is a real entailment, restored when CEm is driven

**CEm silence severs `CeA → LC` (CRF-R1, excitatory — the D6 #2 entailment); LC drops 0.564 → 0.315 under
threat.** This is a real consequence: the LC output leaves from CEm, and a silent CEm delivers no drive to LC.
**Once `BA → CEm` drives CEm, the LC output should be restored — verify it after the fix.** Register: the
`CEm → LC` CRF-R1 pathway (D6 #2) requires CEm to be driven; confirm LC recovers toward its pre-split level once
BA drives CEm. **If it does not fully recover, that is a Stage-B question (whether the LC-projecting CEm output
population is adequately driven).**

---

## 6. Handoff

1. **Add `BA → CEm`** (grounded, glutamatergic, one edge becoming two — parallel to the ITC split). This gives
   CEm the excitatory drive the disinhibitory motif requires. Regrow, gate on the full suite.
2. **Measure:** does CEm now produce output? Does LC recover toward 0.564? Are the six targets in a sane state
   (driven, not crushed, not floating free)? No classification flip.
3. **Then Stage B** — differentiate the CEm output populations (freezing-output → vlPAG-GABA, aggression-output
   → HYPdm, the others grounded to their population), so the BA-driven CEm can route to one output or the other.
4. **Then Stage C** — the CEl SOM+/PKCδ+ selector, which (a) gates the differentiated CEm outputs separately
   (resolving the coupling) AND (b) provides CEl's local inhibition (relieving the CEl saturation Stage A
   created). **The exit measurement closes S56.**

**Register: the suppression-not-competition reframe (the coupling was a GABAergic lump flattening the column,
not one activation serving two modes — verified by Stage A); the `BA → CEm` grounding (BLA projects to both
divisions, parallel to ITC); the CEl-saturation-until-Stage-C prediction (confirmed); and the `CEm → LC`
CRF-R1 entailment (restored when CEm is driven, verify).**

> **Stage A's measurement inverted the assumption we both held: the defensive column's failure was suppression
> by a saturating GABAergic lump, not a competition between two modes for one activation. That sharpens the
> whole un-lumping — CEm must become a DRIVEN node with differentiated, separately-selected outputs, not a
> released one. The scope correction (BA → CEm) is grounded and authorized — it gives CEm the drive the
> disinhibitory motif needs. Add it, verify CEm produces output and LC recovers, then Stage B differentiates
> the outputs and Stage C adds the selector that both resolves the coupling and relieves the CEl ceiling. The
> measurement refuted the story and improved it — as it has all session. Proceed.**
