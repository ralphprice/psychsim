# Lump #13 sequencing — RULING. **Stage B and Stage C are one mechanism. Merge them. Build the selector whole.**

**The recursion finding is correct and I verified it: `CEl-GABA` is itself a lump (one node doing the
SOM+/PKCδ+ job as a blanket brake), wearing a scaffold value sized for a different question, now standing as
the sole gate on the entire defensive output stage. The build session asked whether to invert to A→C→B or fold
C forward into B. The verification shows the real answer is neither: B and C are not separately measurable —
they are one mechanism. Here is the ruling.**

---

## 1. The recursion is real and the value provenance compounds it (verified)

- **`CEl-GABA` is a lump.** Its function string confirms it is the single "CeL GABAergic interneuron
  population" inhibiting CeM — but real CeL inhibits CeM through the **SOM+ and PKCδ+ populations, which are
  mutually inhibitory** (only one active at a time), so CeL's inhibition of CeM is **routed and selective,
  never blanket.** PsychSim models both as one undifferentiated node. **The lumping defect recursed one level
  down — exactly the pattern, now inside the CeL division.**
- **The value compounds it by its own provenance.** The `0.85 "strong"` is a labelled SCAFFOLD sized in v14
  Phase 1.1 to make the disynaptic feed-forward inhibition **dominate one residual PVN-OT→CeA fallback edge** —
  calibrated to beat a single fallback edge onto the pre-split lump. **Re-pointed to CEm, that number is now
  the sole gate on the entire defensive output stage — a load it was never sized for.**

> **The build session did NOT retune it, and correctly so: "the arbitrariness isn't in the number; it's in the
> mechanism the number is standing in for — and Stage C is that mechanism." This is the load-bearing-value
> discipline stated exactly. The blanket 0.85 brake is a scaffold for the selective SOM+/PKCδ+ routing that
> doesn't exist yet. You don't fix the number — you build the mechanism it stands in for, and the number
> dissolves.**

## 2. The sequencing — B and C are ONE mechanism (verified), so MERGE them

**I traced the dependency. Stage B (differentiate CEm outputs) and Stage C (selective brake + selector) are
mutually dependent:**
- **The selective brake (C) must gate differentiated CEm outputs (B)** — a selector needs distinct output lines
  to route between.
- **The differentiated outputs (B) are only measurable once the brake is selective (C)** — splitting a node
  masked to 0.000 by a blanket brake yields two populations that both read 0.000. **B's exit measurement
  ("outputs independently addressable") cannot be taken while the blanket brake masks CEm.**

> **So B and C are not orderable as separate stages — B needs C to be measurable, and C needs B to have targets
> to gate. They are the two halves of ONE mechanism: the winner-take-all selection microcircuit. Splitting them
> into sequential stages was my scoping error — the selector and the outputs it selects among are not
> independently testable. MERGE Stage B and Stage C into a single differentiation build.**

## 3. RULING — build the selection microcircuit WHOLE, as one pass

**The merged final build (call it Stage BC, or simply "the selector build"):**

1. **Split `CEl-GABA` into SOM+ and PKCδ+ mutually-inhibitory populations** (the winner-take-all selector),
   grounded in the CeL selection-microcircuit literature (SOM+ ⊣ PKCδ+; the active population determines which
   CeM output is gated). **This replaces the blanket brake with routed inhibition.**
2. **Differentiate the CEm output populations** — at minimum a freezing-output population (→ `vlPAG-GABA`) and
   an aggression/defensive-output population (→ `HYPdm`), the other outputs (BNST, PVN, RVLM, LC) grounded to
   whichever population the anatomy assigns.
3. **Wire the selector to gate the CEm output populations SEPARATELY** — the active CEl population (SOM+ vs
   PKCδ+) routes inhibition to one CEm output line and releases the other, so the BA-driven CEm produces ONE
   mode at a time.

**The scaffold `0.85` is not retuned — it is REPLACED.** The blanket brake ceases to exist; in its place is
the selective routing of the SOM+/PKCδ+ microcircuit, whose strengths are grounded in the mutual-inhibition
anatomy, not inherited from the v14 scaffold. **The arbitrariness dissolves because the mechanism it stood in
for now exists.** *(If any weight in the new microcircuit needs a value, ground it from the selection-
microcircuit literature — the SOM+/PKCδ+ mutual inhibition and their gating of CeM outputs are characterised —
never inherit the 0.85 or fit to fire an output.)*

## 4. The exit measurement — this closes S56

**The counterfactual already proved the drive works: silencing `CEl-GABA` releases CEm into graded, context-
differentiated output (CEm 0.269 under threat, 0.117 under provocation — 2.3× — with HYPdm/PVN/RVLM biting and
LC recovering to 0.363).** So the drive and the context-differentiation are present; the selective brake is
what lets them through without blanket suppression. **The merged build's exit measurement is the whole S56
close:**
- **Freezing and aggression drivable as distinct selector states** — freezing-appropriate context drives the
  freezing-output (vlPAG-GABA) without the aggression-output being suppressed as a side effect; provocation
  drives the aggression-output (HYPdm/dPAG) — **the two selectable, not competing, not blanket-suppressed.**
- **CEm produces graded context-differentiated output** (the counterfactual's 0.269/0.117 pattern, now via the
  selective brake rather than by silencing it).
- **LC recovers** toward its pre-split 0.564 (the CRF-R1 entailment restored).
- **The two current reds resolve or become informative:** the plain-threat aggress float (`0.0239`, attack
  effectors floating with `CeA→HYPdm` suppression gone) and the freezing floor (`0.050`) should both change —
  the freezing floor especially, since (as the build session noted) pre-split freezing came from the saturated
  lump CRUSHING vlPAG-GABA (blanket suppression), and selective drive should produce freezing properly rather
  than as a suppression artifact. **The reframe showing up in the read-out.**
- **CEl saturation relieved** — the SOM+/PKCδ+ mutual inhibition IS CEl's local brake (the Stage-C ceiling
  deliverable, now part of the merged build).
- **No classification flip** from the restructuring — verify.

## 5. The discipline (unchanged, and the emergence wall is central here)

- **Ground every weight in the new microcircuit** from the selection-anatomy literature; **replace the 0.85
  scaffold, never inherit or retune it.** Any uncertain weight is flagged.
- **★ The selection must EMERGE.** The SOM+/PKCδ+ selector chooses based on the substrate's own drives (the
  balance of afferent drive to CEl, the context) — **never a scripted "if provocation then aggression-output."**
  We wire the mutual-inhibition microcircuit; which population wins, and therefore which CEm output is gated,
  emerges from the drives. **This is the honesty wall at the most load-bearing point of the whole S56 pass — if
  the build finds itself hardcoding which mode the selector picks, stop.**
- **Regrow, gate on the full suite** (the un-lumping is now structurally complete, so the full suite is
  meaningful for the first time since Stage A). **No classification flip.**
- **Measure the exit conditions above** — this is the measurement that closes S56.

---

## 6. Handoff

**Merge Stage B and Stage C — they are one mechanism (verified: neither is measurable without the other). Build
the winner-take-all selection microcircuit whole: split `CEl-GABA` into SOM+/PKCδ+ mutually-inhibitory
populations, differentiate the CEm output populations (freezing-output → vlPAG-GABA, aggression-output →
HYPdm), and wire the selector to gate them separately — replacing the blanket 0.85 scaffold brake with grounded
selective routing. The selection emerges from the substrate's drives; nothing is scripted.**

**Regrow, gate on the full suite (meaningful for the first time since Stage A), measure the exit conditions:
freezing and aggression drivable as distinct selector states, CEm graded and context-differentiated, LC
recovered, the two reds resolved-or-informative, CEl saturation relieved, no classification flip. That
measurement CLOSES S56 — and unblocks the harsh mirror.**

> **The recursion was real: CEl-GABA is a lump wearing a scaffold value sized for a different question, and the
> arbitrariness lives in the missing selective mechanism, not the number. B and C turned out to be one
> mechanism — the selector and its outputs are not separately measurable — so they merge into a single build:
> the winner-take-all microcircuit, built whole, replacing the blanket brake with grounded selective routing.
> The counterfactual already proved the drive is there and context-differentiated; the selective brake lets it
> through. This is the final build of the longest descent in the project. When freezing and aggression are
> drivable as distinct emergent selector states, S56 closes and the harsh mirror — owed since F1–F4 — is
> finally available. Build the selector; let the selection emerge; measure the close.**
