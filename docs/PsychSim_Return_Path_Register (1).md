# PsychSim — RETURN-PATH REGISTER (v3) — **L7 CLOSED**
### Updated at level exit, 2026-07-16. Supersedes v2.

> **v1 was wrong** (built from recent context; dropped the spine). **v2** rebuilt it from the transcript
> record. **v3** closes L7 and opens the climb back.

---

# THE DESCENT — where we are now

```
STAGE 1: v14 kinship/attachment  ── Phases 1 ✓ 1.1 ✓ 2 ✓   [Phases 3-5 OPEN]
   └─ D1. family-psychology dive ✓ (the Arena already is a hothouse)
        └─ D2. DOMINANCE HIERARCHY ── PAUSED (Q1 rank-as-tuning? Q2 override phasing?)
             └─ D3. REPRESENTATIONAL-MEMORY-EXECUTIVE LAYER  ← ★ the purpose; NOT BUILT
                  │   beliefs · attitudes · opinions → RULES, LAW-OBEDIENCE, MORALITY,
                  │   ETHICS, HIGHER EMOTIONS. Memory Phase 1 ✓; PFC loop blocked.
                  └─ D4. LEARNING PATHWAYS ── modeling/imitation NOT STARTED
                       └─ D5. LC / NORADRENERGIC ARC ✓ CLOSED
                            └─ D6. VICARIOUS ROUTE ✓ built [branches open]
                                 └─ D7. EMOTIONAL EXPRESSION ✓✓ **CLOSED 2026-07-16**
```

**The cascade returns to v14 Phase 3, which is where it started.**

---

# L7 — EMOTIONAL EXPRESSION: CLOSED

**Six commits:** `c3d490d` (effector layer: PAG split, `vlPAG-GABA`, `NuAmb` rename+add, `NuFac`) ·
`003768e` + `788635d` (the `motor_effector` domain; the gate class pinned from temperament, kept lesionable)
· `e345788` (the efferent limb: `NRA`; the emotional motor system efferent for the first time) · `2eb5087`
(`BA→dACC` the facial drive, + `dACC-GABA`) · `5a9f23e` (the honest display) · `45fc178` (the volitional
limb: `M1-face`, `PMC-l`, `PAG-PANIC-GABA`). **83 → 93 circuits, 214 → 231 edges.**

### What the level found
- **★ The v9 neutral floor was passing for the wrong reason.** It held because `CeA→PAG(−)` suppressed a
  **lumped** node, while CeA's own citation puts it on the freezing column. **Aggression was not coded — but
  the check that would have caught it was not checking.** → **New principle: a passing keystone certifies the
  OUTCOME, not the mechanism. An artifact can hold a keystone. Always ask what is holding it.**
- **`dPAG-GABA`** — the tonically-active gate that *is* the escape threshold (Stempel & Evans 2024). The
  floor restored **by the real mechanism at its grounded rate**.
- **★ The gate taxonomy** — **pacemaker gates** (`LC`, `dPAG-GABA`: intrinsic rate → ground it → paired
  deviant) vs **recurrent E-I gates** (the cortical interneurons: **no intrinsic rate — ground the LOOP, leave
  the rate scaffold**). → **Principle 6 refined: what is groundable in isolation may be a RELATION, not a
  VALUE.**
- **★ ARTIFACTS INFLATE (§18 widened).** Dead hub (CU signature) · unbraked node (divergence) · fake read-out
  (vicarious magnitude). **Three mechanisms, one direction. Fewer constraints → less attenuation → bigger
  numbers. Smaller-when-honest is the expected direction.**
- **The receptor-sign convention did the work twice** — it forced `CeA→vlPAG-GABA` (Tovote), and it forced
  `PAG-PANIC-GABA` into existence, answering a long-registered question by refusing to let the edge be
  written any other way. **One gate architecture, two opposite functions by source transmitter: GABAergic
  source → release; glutamatergic source → suppress.**
- **Four construct-validity catches, all before they became findings** — the throttle∩display overlap · the
  effectors inheriting `defensive_threat` · **the amygdala's only route to the face running through the
  network CU spares (would have INVERTED the signature)** · the domain-as-throttle-surface auto-extension.
- **Two anti-tuning patterns worth reusing:** the **byte-identical twin** (`dACC-GABA`'s loop bands copied
  from `dlPFC-GABA` — *no degree of freedom in which to tune*) and the **schedule reference**
  (`pfc_low_early_high_late`, pre-existing on 38 edges — a reference, not an authored curve).

### ★ The three results — measured, small, nothing coded
1. **`vicarious < direct` re-derived off the honest display and HELD.** Magnitude shrank, ordering held.
2. **S22 — the modality dissociation SURVIVED THE CROSSTALK.** `M1-face` reaches both effectors, and
   pain→face / separation→voice still held. **A finding, not a topology.**
3. **★ THE DUAL-PATHWAY DISSOCIATION EMERGED — the psychopathy expression signature.** Involuntary blunted
   under affect-throttle, volitional spared. **`vlPFC→PMC-l` was load-bearing** (vlPFC ∉ `AFFECTIVE_EMPATHY`)
   — the only reason it was measurable at all.
4. **★ THE HEADLINE — a psychological theory emerged from the connectome.** **Gross's reappraisal vs
   expressive suppression fell out of WHERE two routes land**: `vlPFC→ITC` reaches the amygdala's gate →
   *feeling* down; `PMC-l→PAG-PANIC-GABA` reaches the expression column → *display* down. **Nobody coded
   antecedent- vs response-focused regulation. The distinction IS the anatomy.**

### Branches L7 CLOSED
✅ chronic-distress invisibility (display reads effectors above a fixed rest) · ✅ contagion damping
(**damped, not saturated** — tail 0.67–0.72, loop gain ≪1) · ✅ **the per-edge-phasic register — all 4
settled** · ✅ the construct-validity overlap (**dissolved on its own; the throttle set never touched**) ·
✅ `PAG-PANIC`'s own gate (the convention forced it)

---

# OPEN BRANCHES

### Opened by L7
| item | note |
|---|---|
| 🔴 **S29 — `M1-face`'s socket** | afferent-less **by design**: its afferent is a **learned display rule**, which lives in **D3**. **A socket, not a defect. The first hook of the climb back.** |
| 🔴 **The cortical brake layer is 3 of 11** | 8 unbraked nodes, quiet only because their afferents are. **Every one saturates when given drive.** Carries the effect-sizes-biased-upward caveat. |
| 🔴 **The defensive columns are wired for permission, not drive** | `vlPAG` = 0.000; `VMH` has no excitatory afferent. **First suspect: `MeA→VMH` on the transmitter FALLBACK while `MeA→VMHvl` is AMPA.** Merge with `dPAG-GABA`'s missing release. **`DEFENSIVE_OUTPUT` has been computing on 2 of 3 terms.** |
| 🔴 **The read-out audit** | now a class: `DEFENSIVE_OUTPUT` (2 of 3) · the executive read-out (conflates monitoring with control). **Same check as the throttle-set audit, from the other side.** |
| 🔴 **S28 — the autonomic cost of suppression does not emerge** | principle 1: a missing pathway. Own grounding pass. **The cost is not claimed.** |
| 🔴 **S23 — `IN-PROPRIO` is unwired** | §3.2's efference-copy/agency mechanism does not exist. **No agency claim until it lands.** |
| 🔶 **Four split candidates** | **`NuFac` upper/lower (HIGHEST VALUE — the Duchenne marker; "displays without feeling" IS a lower-without-upper signature)** · `dPAG` dl/l · `dACC` cognitive/motor · `PMC-l` |
| 🔶 **The extrapyramidal route to the face is absent** | we have the cortical involuntary route only. Disclose against the dual-pathway result. |
| 🔶 the divergence at 0.0534 (xfail, self-clearing) · the capacity measure at matched demand (xfail) · preBötC + respiratory rhythm · the inspiratory PAG route · `SC-Pv→dPAG` 0.50 (Point-1) · `NuAmb-vocal` display cannot distinguish cry from speech · `PAG-PANIC`=dm identification · scan-vs-signature disjointness | |

### Pre-existing
🔴 **the reactivity/regulation fusion** (the gates ARE the regulation; one dial moves both — **the substrate
cannot express low reactivity with high self-command, which is the sophropathy phenotype.** Registered
**above** the CU study) · 🔴 CeA operating point · 🔴 throttle-set audit + construct-validity check ·
🔴 self-regulation mechanism · 🔴 82 uniform setpoints · 🔶 BF-ACh · 🔶 SNc · 🔶 R4 plasticity guard ·
🔶 `reward_signal()` · 🔶 the U-shape · 🔶 teen≤child · 🔶 PBN/NTS→LC · 🔶 reward-completion · 🔶 v15 ·
🔶 epigenetics

---

# THE CLIMB BACK — begins now

1. **D6 — vicarious branches.** The **throttle-set audit** (cheap, independent, study-critical) · the **CeA
   operating point** (via the self-regulation mechanism) → **then** life-course accumulation becomes
   answerable.
2. **D4 — modeling/imitation** → learning pathways COMPLETE.
3. **D3 — the representational layer.** The PFC↔memory loop CLOSES (a learned control-disposition now
   exists) → **★ the higher-order read-outs: beliefs, attitudes, morality, rules, higher emotions — the
   layer's purpose** → **and `M1-face`'s socket takes its afferent: the learned display rule.**
4. **D2 — dominance hierarchy.** Answer Q1/Q2; rank as emergent circuit-tuning; sign `dmPFC→LA`.
5. **Stage 1 — v14 Phases 3, 4, 5** + the three emergence tests → **v14 COMPLETE.**
   *(`NRA` is already the premotor node Phase 5's reproduction wiring needs — the expression limb built part
   of it on the way down.)*
6. **Stages 2–7** — v15 → daily life-course & multi-environment → psychometric observer → **the 209-edge
   audit (the gate)** → **the CU study** → **the interventions.**
7. Then: the parallel episodic version; the anatomical reference doc (= the audit made readable).

**Discipline: update at every level exit. Do not exit a level with unlisted branches. We do not drop out of
the cascade early.**
