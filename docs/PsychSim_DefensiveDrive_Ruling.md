# The defensive-drive grounding — RULING

## 1. ★ I over-corrected. Your sweep settled what neither of my verdicts had measured.

**The record, plainly:**
- **My first verdict: `TRUE BY ANATOMY` — "stop trying to dissolve it."** Too *closing*.
- **My correction: `UNRESOLVED` — "dPAG is inoperable, blocked on the release gap."** Too *opening*.
- **Your sweep: the gate is SURMOUNTABLE at ~0.7. Observation drives `SC-Pv` to 0.584 — sub-escape.**

**So `dPAG` is not broken and there is no missing release. `dPAG` correctly declines to relay observation
because observation is correctly sub-escape: you do not flee from watching.**

> **Both my verdicts were inferred. Neither was measured. One sweep settled it.** That is **S35 turned on me —
> *the stopping rule is the measurement* — and I ruled twice without one, in opposite directions, from the same
> chair that wrote the rule.

### ★ REINSTATE the split verdict — and it is now far stronger than the one I withdrew
- **VISUAL: `TRUE BY ANATOMY` — and now MEASURED, not inferred from an afferent list.** The gate is
  surmountable; observation lands at 0.584 against a ~0.7 threshold; **and the threshold that produces that is
  GROUNDED ELECTROPHYSIOLOGY (6.2/32.2 Hz → 0.19).** **So "observed distress does not reach arousal via the
  escape column" is a grounded, measured, emergent fact — not a gap.** *(Your S37 refinement is the finding:
  `dPAG` is escape-gated, and observation is correctly sub-escape. Nobody coded that; a grounded rate
  produced it.)*
- **AUDITORY: still live** — the injection depth. Unchanged.

---

## 2. ★ The missing trigger is real — and it is a CLASS, with an actionable

**Verified: there is no `IN-VIS:looming` channel.** The full visual input surface is `IN-VIS`,
`IN-VIS:biological_motion`, `IN-VIS:face_like`. **And `SC-Pv → dPAG` is `basis: innate_reinforcer`, citing
PB-LOOMING.**

> **The looming reinforcer is in the catalogue, cited — and has no input channel. The model has the escape
> gate (grounded) and the relay (grounded) and NO TRIGGER. The escape circuit has never been exercised by a
> real escape cue.**

**And it is the class `IN-PROPRIO` belongs to (S23): the INPUT SURFACE has gaps, and they are invisible
because a missing channel produces a NULL, not an error.**

> **ACTIONABLE — cheap, mechanical, do it in this pass: audit the CATALOGUE against the CHANNELS. Every cited
> innate reinforcer must have an input channel.** The escape trigger is the first thing it finds; `IN-PROPRIO`
> is the second; **and every one it finds is a mechanism that silently cannot fire.**

---

## 3. ★ Your lump-detector at scale — and the filter that makes it a detector

**I applied it: 161 of 236 edges are on the transmitter fallback — 68 %.** But the raw count is not the
finding, because **the fallback is CORRECT for a homogeneous source**: `RET → LGN` runs on it, the retina is
glutamatergic, the fallback gives +, and that is right.

> **The detector's precise form is yours plus one filter: `dominant_receptor: None` **AND** a source whose
> transmitter field names TWO transmitters.** `MeA`'s **"GABA/glutamate"** is the archetype — **not an
> ambiguity, but the lump written into the data, with the fallback resolving it by coin-flip.**

**Sweep for that and you get a LUMP CENSUS, generated mechanically.** **We have found eleven lumps by
accident — one at a time, each because something broke.** **The detector finds them by query, before they
break anything** — and it is a direct, mechanical pre-computation of the 209-edge audit's WRONG-TARGET /
MISSING-ELEMENT categories. **Run it with the catalogue audit; report, fix nothing.**

---

## 4. Your self-caught null is the best thing in the report

> *"the first sweep drove `IN-VIS:looming`, which doesn't reach `SC-Pv` … so `SC-Pv` stayed at 0.13 and
> nothing crossed. That's a null from a manipulation that never engaged the mechanism."*

**That is construct-invalidity in its purest form — a null indistinguishable from a real one — and you caught
it in your own work, before reporting, by checking the source's channels.**

**And note precisely what it would have concluded: "the gate is insurmountable → a release is missing" —
which is the answer I had already ruled toward.** **Your check overturned my hypothesis and your own first
result in a single move.** Had it not been made, we would have registered a missing release that does not
exist, gone looking for a forebrain source the literature does not name, and never found the trigger.

---

## 5. Order — unchanged, plus two cheap sweeps
1. **The `MeA`/`VMH` grain diagnostic** — both edges, one pass, **keystone-bound, surface before building.**
2. **The catalogue-vs-channels audit** (§2) and **the lump census** (§3) — both mechanical, both report-only.
3. Then `vlPAG`'s drive, re-run on the un-lumped node.
4. Then door 1's auditory channel (the injection depth + the `AUD-brainstem` grain).

**Door 1: visual `TRUE BY ANATOMY` (measured), auditory live. #2 open on doors 2 and 3.**
**The release stays `UNRESOLVED` — and it is now unresolved *and unneeded*, which is a better place than we
were.**
