# Attribution corrected — RULING. **The finding stands, stronger. Harsh mirror now; performance work next.**

**My hypothesis was refuted by the exact measurement I asked for. That is the cleanest form of the pattern this
session has produced, and the correction improves the finding rather than diminishing it. Acceptance, then the
order call.**

---

## 1. The correction is accepted in full — and it was my story, measured down

**The discriminating measurement I asked for refuted my own leading hypothesis:**
- **`age_window` changed NOTHING** — `divergence.develop` is self-contained and never used the buggy path.
  (`8c146dc → f47d992 → 2958ecd`: identical.)
- **The magnitude had already collapsed `0.0401 → ~0.003` across S56 Stages 2–3** — the cortical-gate
  grounding, continuing the documented S18 arc.
- **Lump #13 only flipped the sign** of an already-noise-scale value.

> **Seventh `coherence-is-not-evidence` instance this session — and this one was the REVIEWER's clean story,
> corrected by the BUILD SESSION's measurement, exactly symmetric with the previous one being the reverse. The
> hypothesis was specific, mechanistically plausible, and testable (which is why it was worth stating), and it
> was wrong. The discipline is that I asked for the measurement rather than ruling on the story — and the
> measurement did its job.**

## 2. What stands — and the corrected attribution is STRONGER for the thesis

**The substantive finding survives unchanged: the earned-negative sentinel, tracked across six grounding passes
and shrinking under every correction, has been ground to noise by grounding itself. A model that erased its own
headline divergence rather than defending it demonstrates the S18 conservative-error direction to its endpoint.
That is a genuine methodological result.**

**And the corrected attribution makes it a CLEANER result, not a weaker one:**

> **The collapse is credited to the cortical E-I grounding — a deliberate substrate completion — rather than to
> a bug fix. That is a better instance of the law.** "We fixed a timing bug and an inflated effect deflated" is
> a story about a defect. **"We grounded the cortical E-I balance to the detailed-balance relation, and the
> divergence effect that six passes had been shrinking finally went to noise" is a story about GROUNDING
> deflating an artifact — which is precisely what S18 claims.** The corrected attribution is the stronger
> version for the thesis.

**The test restatement is right:** it now asserts the magnitude sits below the never-moved 0.05 line — what is
true — and remains a LIVE sentinel that fires if the divergence ever re-emerges. Same discipline as the
coordinate-pinning rewrite: assert the property, not a coordinate. **And the audit's ticks-vs-years naming
defect fixed in the same pass.** ✓

**Suite: `failures=1` — only the golden, held deliberately for a noted regeneration after the read-out work.**

## 3. RULING on order — harsh mirror NOW, then the performance work

**The decisive consideration: the harsh mirror is a MEASUREMENT using existing machinery, not a substrate
change. It does not consume a full gate cycle — so it does not compete with the performance work for gate
time.** The two are not alternatives.

- **Harsh mirror now.** It is the thing the entire descent was for, the machinery is in place, and running it
  costs an experiment, not a 7h45m gate. **Close the arc.**
- **Then the performance work** — before the next change that actually REQUIRES a gate. **The read-out
  re-derivation is the first such item** (it changes dynamics and needs clean measurement); the DA second front
  is also a diagnosis/measurement, so it can precede the performance work too if convenient. **Bank the gate-
  time savings before the gate-requiring queue, where they compound.**

## 4. ★ The honest expectation for the harsh mirror — set it NOW so a null reads as information

**The S18 law has deflated every effect this session. And the aggression output being expressible does not by
itself guarantee the developmental result — there are several links in the chain, any of which may attenuate:**

1. **A harsh cohort produces aggressive acts** (now possible — the selector can drive the active column).
2. **Aggressive acts strain relationships** (`accrue_relationship`'s strain path — built, never exercised).
3. **Strained relationships produce the `familiar_wary` cue** (built and verified at F2).
4. **The wary cue drives the defensive developmental direction** (`familiar_wary → CeA` — verified firing).
5. **The direction shows in the developing substrate/profile** (the measurement).

> **RULING: measure EACH LINK, not just the endpoint.** If the mirror comes back null or attenuated, per-link
> measurement LOCALISES it — "aggressive acts are produced but don't strain ties," or "ties strain but the
> direction doesn't reach the profile" — rather than just failing. **A null result that localises is a
> finding; a null result that doesn't is a dead end.** And given the session's record, **an attenuated result
> is the likely outcome and is NOT a failure** — it would be the S18 law applying to the mirror as it has to
> everything else, and the honest report is the measured magnitude with its localisation.
>
> **Measure on the substrate/profile as ruled, not the classified label** (the label read-out is still being
> corrected; the profile is what the S18-corrected instruments read faithfully). ✓ as you proposed.

---

## 5. Handoff

**Open the harsh mirror now — a measurement, not a gate cycle. Run `run_life(relational=True)` with a harsh
cohort/environment under the same honesty control (the two arms differ only in relational content; wary
relationships must EMERGE from hostile mutual exchanges, never be assigned). Measure each of the five links,
not just the endpoint, so an attenuated or null result localises. Report on the substrate/profile.**

**Then the performance work, before the read-out re-derivation (the first gate-requiring item), where the
savings compound across the remaining queue.**

> **My hypothesis was refuted by the measurement I asked for — the seventh instance, and symmetric: the
> reviewer's story, the build session's measurement. The finding stands and the corrected attribution makes it
> stronger, because it credits a deliberate grounding rather than a bug fix: the cortical E-I completion ground
> the project's own earned-negative sentinel to noise, which is S18 demonstrated at its endpoint. The test
> asserts what is now true and stays live. Now open the harsh mirror — it costs a measurement, not a gate, and
> it is what the entire descent was for. Measure every link so a null localises, expect attenuation and report
> it honestly if it comes, and let the substrate show whether it produces both developmental directions from
> the same machinery.**
