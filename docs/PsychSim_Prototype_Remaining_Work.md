# Remaining work to the first prototype — and what is next

**Fair criticism. The prototype goal receded behind the grounding descent. Here is the honest position, what
remains, and the declaration.**

---

## 1. Where we actually are

**The substrate is done.** 101 circuits, 265 connections. The defensive system is grounded end to end (four
cortical E-I gates, the CeA selector, the CRF+/SOM+ competing pair, the freezing and active columns both
drivable). The four-arc loop closes: perception in, substrate, expression out, the relational record. The
descent that consumed this session was the last structural hole in it, and it is closed.

**The study layer is the remaining work, and it is smaller than what we just finished.** The audit said this
precisely: the substrate is more disciplined than the instruments that read it. We fixed the instruments'
*read-outs*. What is left is the study layer's own defects and the one output change that makes its results
trustworthy.

**What the grounding bought, so it is not mistaken for a detour:** the aggression output is expressible (the
study can now produce both developmental directions); the instruments no longer multiply the manipulation into
their own results; three of four emergent findings are confirmed on corrected instruments; and gate time went
from 7h45m to 74 minutes, which makes everything below cheap.

## 2. What "first prototype" means

**An end-to-end life-course study run that produces trustworthy, honestly-measured developmental outcomes,
with no banned patterns in the study layer.** Concretely: a cohort develops through the coupled loop under
manipulated moral and relational environments, and the study reports graded developmental profiles that a
reviewer can trust. That is the instrument the six thesis studies are run on.

## 3. What BLOCKS the prototype

**(a) The classified outcome is a bare argmax.** `readout.py:114` takes `max(prof)`; `LifeResult` stores only
`classification: str` and discards the profile; `margin`/`runner_up` exist and are read by nothing. **This has
now demonstrably destroyed one finding (Buckholtz), hidden another (F4's real +0.0088), exaggerated a third
(the fragile psychopathic flip), and broken a fourth test. The study's primary output is a lossy
discretization.** Until the study reports the graded profile with its margin, no result it produces is
trustworthy. **This is the single highest-value remaining change and it is cheap.**

**(b) Two banned patterns still in the study layer.**
- `punishment_sensitivity = 0.3 + 0.7·fear` — a hand-authored trait→outcome mapping standing as the headline
  learning result. The real yoked-control probe exists and is sound; wire it in.
- **Intergenerational transmission** — three hand-authored coefficients (`warmth = 1.3·care`), the parent never
  instantiated as an agent, and `PARENT_SEEDS` feeding a developed psychopathic end-state in as a *causal
  input*. This is the outcome-category-as-primitive violation, and it is load-bearing for the Stage-7
  transmission claim. **Ground it or declare the claim withdrawn.**

**(c) CU ≡ 1 − empathy** — report one, not both. Trivial, but it is a figure reporting one variable twice.

**(d) Experiments that report structural zeros** — the justice experiment counts impossible keys (all 0.0%),
the bifurcation sweep bisects to 1e-3 on a profile whose top-two gap is 1.6e-4. **Both produce
precise-looking output that means nothing.** Fix or retire.

## 4. What does NOT block the prototype (after)

The adult-plasticity floor grounding; the two integrity sweeps; the `gaps_register` structural pass; Route 3
(environment → exchange percept); the three-store reconciliation (gates P3/population scale, not the first
prototype); the warmth-display ARC-3 effector; OFC→DA grounding; the dCA2 and dl/l-PAG un-lumpings; the memory
layer's registration; the cue-alone and CEl-discrimination questions.

**All registered, none of them stand between here and a working study run.**

## 5. DECLARED — the next three, in order

**1. Wire the graded profile through the study output.** `LifeResult` keeps the profile; `classify` returns
label + margin + runner_up; the study reports the graded profile, and any label-level claim carries its margin.
**This is next, and it is next because every study result the prototype produces is untrustworthy until it
lands.** Re-state the affected claims on the graded output as part of it — including the link-5 harsh-mirror
magnitude, which is currently stated on a superseded instrument and gets re-measured in the same pass.

**2. Clear the two banned patterns.** `punishment_sensitivity` → the yoked probe. Intergenerational
transmission → grounded, or the Stage-7 claim withdrawn and recorded. Plus CU-or-empathy in the same commit.

**3. Fix or retire the structural-zero experiments** (justice keys, bifurcation tolerance), then run the
**first end-to-end prototype study** on the corrected instrument and report its output. **That run is the
prototype.**

One gate covers items 1–2 (both study-layer, non-interacting with each other's measurement); item 3's
prototype run is a measurement, not a gate.

---

> **The substrate is finished; the study layer is what remains, and it is a shorter list than the descent we
> just closed. The prototype is blocked on one thing above all — the study reports a bare argmax over a graded
> profile, which has already destroyed one finding and hidden another. That is next: wire the graded profile
> through the output, re-state the affected claims on it, then clear the two banned patterns, then run the
> study end to end. Three steps to a prototype that produces results a reviewer can trust.**
