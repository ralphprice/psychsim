# The fallback-sign census — LOAD-BEARING, not a list (report only, nothing built)

The register says "161 ungrounded signs." That number is not actionable and it hides the real shape. The
census's question is sharper: **which fallback-signed edges are LOAD-BEARING ON A GREEN TEST — where a passing
test certifies an outcome resting on a sign nobody chose?** Scoped per S44 to the hazardous set (fallback edges
leaving multi-transmitter sources; single-transmitter glutamatergic fallbacks return +1 and +1 is correct —
ungrounded, not hazardous).

**Method (column 3, the point):** for each of the 38 fallback edges from the 28 multi-transmitter sources,
**flip the sign** and ask whether a *meaningful* green changes colour — a keystone assertion, the freezing
floor, or a develop→classify **classification label**. Reduced develop coverage (soph×{warm,harsh},
psy×{warm,harsh} — the study contrast) for tractability. **No re-signing, no re-banding: every sign that needs
changing needs a citation, and citations are the reviewer's.**

---

## ★ The result: the hazard is ONE edge, not 161

| tier | count | edges |
|---|---|---|
| **load-bearing on a green, sign GENUINELY AMBIGUOUS** | **1** | **`MeA → ATL-TP`** |
| load-bearing on a green, sign CORRECT-and-explainable | 2 | `LA → BA`, `LA → CeA` |
| **hollow / inert** (sign-flip changes no meaningful green) | 35 | the rest |
| load-bearing on a RED | 0 | — (no fallback sign flips the freezing floor's red) |

### ★ THE PRIORITY — `MeA → ATL-TP` (flag for the reviewer; do NOT resolve)
- **Column 1 (fallback?):** yes — `dominant_receptor: None`.
- **Column 2 (sign):** currently **−1**, set by `MeA`'s transmitter field `"GABA/glutamate; steroid-sensitive"`
  → GABA-first typing order → −1 (S44). `MeA` is a *genuine* two-transmitter lump (both MePV/MePD populations
  project; not the local-GABA resolution). **What it actually projects with is the reviewer's literature.**
- **Column 3 (load-bearing):** ★ **flipping the sign changes the sophropathic child's classification label
  `executive → social_cognition`** (soph×warm and soph×harsh; reproducible). **A CU-study developmental
  read-out — the thesis's actual output — rests on a sign that a string's typing order chose.** This is the
  worst object the census set out to find. It is one of `MeA`'s three known-hazardous edges (S43); now
  confirmed the load-bearing one.
- **Ruling constraint honoured:** load-bearing on a green, and I cannot tell which way it should point →
  **left, flagged, reviewer fetches it.** NOT resolved by which sign keeps the label.

### The two correct-but-ungrounded (grounding list, not hazard)
- `LA → BA` (load-bearing on keystone assertion #1, threat→avoid) and `LA → CeA` (assertion #5,
  efficacious+restrained) both rest on their **+1**. But `LA` is `"glutamatergic input; local GABAergic
  interneurons"` — the projection is glutamatergic (the GABA is local, does not project), so **+1 is
  correct-and-explainable** (the `VMH→vlPAG` resolution). Ungrounded but not hazardous → pin the receptor
  (AMPA) when convenient; no sign question.

---

## The 35 hollow — a fourth independent sighting of the same defect
Sign-flip changes no keystone assertion, not the floor, no classification label. Their fallback signs do not
matter to any meaningful outcome — the `AFFECTIVE_EMPATHY`/`HYPdm` hollow shape, now found from a **fourth**
direction (receptor-sign convention → read-out audit → Q1 baseline sweep → this). Includes all five `PVN`
autonomic efferents (`RVLM`, `IML`, `DMV`, `NuAmb-cardiac`, `Pituitary`) and most sensory relays.

## SPLIT-BLOCKED candidates (lump → lump — record, do not force a sign)
Both endpoints two-transmitter, so a *single* sign cannot be right (segregated sub-projections, the
`MeA→BNST` shape): **`MeA→BNST`** (confirmed), `MeA→MPOA`, `MPOA→VTA`, `HYPdm→PVN`, `BNST→HYPdm`, `PPTg→VTA`,
`VN→S1`. **All are INERT** — no green rests on them — so recording them SPLIT-BLOCKED is cheap and none is
urgent. Each still needs the reviewer's anatomy to confirm it is genuinely dual-projection vs single-dominant.

## ★ Meta-finding: the characterisation golden cannot be the load-bearing discriminator
A first pass on *silencing* + the profile-level golden flagged **33/38** edges — because the golden asserts
**exact 4-decimal equality**, so nearly any perturbation fails it. That is a hair-trigger, not a measure of
load-bearing: it certifies exact reproduction, not meaningful outcomes. The discriminating greens are the
**keystone, the floor, and classification LABELS** — and on those, the count collapses from 33 to 3. (Worth
its own note: the golden guards against *drift*, but it cannot rank *importance*.)

---

## What it feeds (the number Ralph needs)
Of 161 fallback edges: ~123 leave single-transmitter glutamatergic sources (+1 correct, S44); 38 leave
multi-transmitter sources; **of those 38, exactly ONE is a genuinely-ambiguous sign load-bearing on a
meaningful green.** So the fallback-sign *hazard* — the part that can silently certify a wrong outcome — is
**one edge (`MeA→ATL-TP`), plus a handful of lump→lump SPLIT-BLOCKED cases that are all inert.** The 209-edge
audit's fallback-sign component is **tens to look at, one to fix under pressure** — not hundreds. Nothing was
re-signed, re-banded, or built; `MeA→ATL-TP` is flagged for the reviewer's citation.
