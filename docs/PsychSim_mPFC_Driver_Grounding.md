# The mPFC driver — GROUNDED. **It is not the driver.** And a bigger finding underneath it.

---

## 1. `dmPFC → vlPAG` is real, cited, excitatory — and it is a BRAKE on freezing, not its drive

**Rozeske, Jercog, Karalis, Chaudun, Khoder, Girard, Winke & Herry — *Neuron* 2018;97(4):898–910**, single-unit
recordings + optogenetics, on exactly this projection:

> **"a neuronal subpopulation in the dorsal medial prefrontal cortex (dmPFC) that projects to the lateral and
> ventrolateral periaqueductal gray (l/vlPAG) and is selectively activated during contextual fear
> discrimination. Moreover, optogenetic **activation and inhibition** of this neuronal population promoted
> contextual fear **DISCRIMINATION and GENERALIZATION**, respectively."**
> **"…control SWITCHING BETWEEN DIFFERENT EMOTIONAL STATES during contextual discrimination."**

**Activate it → the animal discriminates → LESS freezing in the neutral context. Inhibit it → the animal
generalises → MORE freezing.** **It brakes inappropriate freezing. It does not drive freezing.**

### And the sign arithmetic forces the target cell — you called this exactly
- `dmPFC` is cortical → **glutamatergic** → excitatory.
- Activation → **less** freezing.
- **A glutamatergic source that produces LESS freezing cannot be landing on `vlPAG`-glut. It must land on the
  GABAergic selector.**

> **`dmPFC → vlPAG-GABA`. The THIRD instance of the architecture** — `vlPFC→ITC`, `PMC-l→PAG-PANIC-GABA`, now
> this. **And note what it is: not the driver you were looking for, but the SELECTOR'S THIRD AFFERENT.** It
> advances step 2, not step 1. **The convenient candidate resolved into a different, correct thing — the
> `dPAG→PGi` pattern again.**

**A second, separate arm exists — register it, do not build it:** *"an **excitatory descending neural
pathway from the dmPFC to the vlPAG**… activation produced **analgesic and antianxiety** effects"* (JCI 2022,
rabies-based transsynaptic tracing). **That arm lands on `vlPAG`-glut, and its function is descending
ANALGESIA (vlPAG → RVM), not freezing.** **No RVM exists in the model. Blocked, named, registered.**

---

## 2. ★ The finding underneath: **`vlPAG` HAS NO FREEZING OUTPUT**

I checked its efferents while grounding the driver:

```
vlPAG <- vlPAG-GABA, VMH, DRN
vlPAG -> NRA            ← and NRA is "the PAG's PREMOTOR RELAY to the VOCAL/EXPIRATORY motoneurons"
```

**Its only efferent is the vocalisation relay. And Tovote's freezing route — `vlPAG → Mc`, the magnocellular
nucleus of the medulla — has no node. No `Mc`. No `RVM`. Nothing.**

> **We have spent this entire sub-branch giving `vlPAG` its DRIVE. Even if we succeed, FREEZING WILL NOT
> HAPPEN, because `vlPAG` HAS NOWHERE TO SEND IT.**

### And the target is named in the sentence we already cite
The Tovote citation behind `CeA → vlPAG-GABA` reads, in full:

> *"an inhibitory pathway from the central nucleus of the amygdala to the ventrolateral periaqueductal grey
> that produces freezing **by disinhibition of ventrolateral periaqueductal grey excitatory outputs to
> pre-motor targets in the magnocellular nucleus of the medulla**."*

**We built the first clause and not the second. The freezing effector is IN THE CITATION.**

> **PARTIAL COMPLETION RECORDED AS COMPLETION — the fourth instance, and the sharpest. Not a hub whose
> afferent list we never checked: a single cited sentence whose second clause we did not build. And I cleared
> it — Phase A, and every vlPAG ruling since.**

---

## 3. What this changes

**The freezing floor's red is the OUTPUT gap AND the drive gap — and the output gap is PRIOR.** A driver for a
node with no relevant efferent produces nothing. **Q2 was necessary and never sufficient, and we could have
found that at any point by asking what `vlPAG` projects to.**

**And it reframes `DEFENSIVE_OUTPUT`'s hollow.** It reads `CeA`, `vlPAG`, `BA` — **the felt state and the
column. Not the effector.** **That is precisely the defect `_DISTRESS_DISPLAY` had, which Phase C fixed by
pointing the display at the effectors.** **We never applied the same fix here.** My earlier ruling — *"do not
re-point it; give `vlPAG` its drive"* — was right and incomplete: **the honest read-out is the effector, and
the effector does not exist.**

---

## 4. The order — revised
1. **★ `Mc` — the freezing premotor.** Cited in Tovote, in the sentence already in the seed. **Apply the
   AdrenalCortex test first: HUB or TERMINAL?** Its efferent is the spinal motor system, which does not exist
   — **so `Mc` is almost certainly a TERMINAL, i.e. an EFFECTOR, the exact class as `NuFac` and
   `NuAmb-vocal`. Which means it is where freezing gets READ.** Domain `motor_effector`. **This is L7's
   opening move again: the emotional motor system was unefferented, and so is this one.**
2. **`dmPFC → vlPAG-GABA`** — the selector's third afferent (Rozeske; sign forced by the behaviour, not
   assumed).
3. **The driver — still unfound, and the mPFC is not it.** Remaining: `PBN`, `SC-Pv` (both exist and are
   afferented). **And re-ask the question honestly: does `vlPAG`-glut need a driver beyond `VMH`, or is `VMH`
   the driver and the whole failure was the missing output?** **Measure that before adding anything.**
4. The opioid system — its own pass.

**`DRN → vlPAG` and `VMH → vlPAG` still untouched.** **And the freezing floor's red now has a better one-line
reason: `vlPAG` has no freezing effector, its selector has two of its afferents, and its driver is unconfirmed
— in that order.**
