# The `dlPFC` fork — **RULING: (b), keep it deferred. And ground `DRN` anyway.**

**Q1 landed exactly right: a receptor pinned, sign unchanged, band untouched, library byte-identical. A pure
grounding with zero behavioural consequence — which is what a grounding should look like when the sign was
already correct. And you staged only the seed.**

---

## 1. First — **your measurement falsified my REASONING, not just my prescription.** That is the headline.

I wrote: *"`LHb → DRN` becomes a modulation on a large tonic rate rather than the whole signal — exactly what
happened to `CeA → LC` when `PGi` landed."*

**That is wrong, and it is wrong mechanically:**
- **`CeA → LC`: the fix ADDED AFFERENTS. That diluted `CeA`'s SHARE. The share fell because the denominator
  grew.**
- **`DRN`: raising a BASELINE dilutes nothing. In an additive engine the delta is unchanged and the floor
  just rises.**
- **Same sentence. Different mechanism. The second one does not work, and your measurement is what found
  that.**

> **My six earlier errors were stale FACTS — a field, a set, a count. This is a STALE ANALOGY: a precedent
> that was true in its own case, applied to a structurally different one without checking the mechanism.**
> **It is more dangerous than a stale fact, because a stale fact is checkable against the file and a stale
> analogy SOUNDS LIKE REASONING.** **A precedent is a hypothesis, not an argument.** Record it — it is the
> seventh, and it is a different species.

---

## 2. The fork: **(b). And (a) is worse than "premature" — it is a tune at family scale.**

**(a) requires a band we cannot ground.** The real cortical E-I loop's inhibition **scales with drive** — ours
is one number, inherited byte-for-byte across four gates. **To stop `dlPFC` saturating we would have to pick a
new number, and the only thing recommending any particular number is that it turns the freezing floor green.**

> **That is the `BA→dACC`-to-"low" move at FAMILY SCALE — choosing a band for the entire gate family because
> one test passes.** The test we always apply: **would we make this change if it did NOT turn the floor
> green?** **Yes — `dlPFC` saturating to 1.0 is wrong regardless.** **But WHICH change? We do not know, and
> *"the one that works"* is not an answer.** **S56 stays deferred to its own grounded, family-wide pass.**

## 3. But **ground `DRN` anyway** — and this is the part to be firm about

> ***"The ruled fix makes freezing worse"* is not a reason not to fix a known defect. It is information about
> what the result was resting on.**

**`DRN` is a pacemaker-class node at a driven-class baseline. That is true, measured, and independent of what
it does to freezing.** And grounding it does not break freezing:

> **It REVEALS that freezing was only ever going to work because two scaffold values happened to cancel — an
> ungrounded-low brake (`DRN` 0.05) against an ungrounded-low drive (`VMH→vlPAG`, `low-moderate`). Ground one
> and the other's arbitrariness shows.** **That is principle 2 arriving exactly on schedule: a load-bearing
> value means the arbitrariness is in a mechanism it compensates for.**

**Build it: `DRN`'s baseline grounded (1–2 Hz clock-like; `LC`/`dPAG-GABA` precedent for normalisation;
setpoint paired). `LHb → DRN` untouched. `DRN → vlPAG` untouched. `VMH → vlPAG` untouched.** **And expect the
floor to stay red — for a better reason than it is red now.**

## 4. ★ And record what your measurement says about the TEST itself

**The floor's colour depends on which defect we fix first:** fix `dlPFC` alone → green. Ground `DRN` alone →
redder. Both → unknown.

> **A test whose colour depends on the ORDER in which we fix unrelated defects is not measuring the
> mechanism. The freezing floor's positive half is currently a test of whether THREE SCAFFOLD VALUES
> BALANCE — `DRN`'s baseline, `VMH→vlPAG`'s band, and the gate family's inherited bands. Its red is not yet
> informative, and its green would not have been either.**

**Write that into the test.** It does not stop being the right test — **it stops being a test of freezing
until those three are grounded, and it should say so.** **That is the both-halves fix doing its job one level
up: we built a floor that could be passed by a corpse, fixed it, and have now found it can also be passed by
a coincidence.**

**And note the §18 reading, which is the reassuring part: the honest direction is redder. A green here would
have been the surprise worth stopping for.**

---

## 5. Order
1. **Ground `DRN`'s baseline** (§3). Floor stays red; re-attribute it precisely.
2. **`VMH → vlPAG`'s BAND is now exposed as load-bearing and ungrounded** — **that is the next grounding
   question, and it is now askable honestly**, which it was not while the brake was also scaffold. **Do not
   crank it. Ground it.**
3. **S56 — the gate family's calibration — its own pass, grounded, family-wide.** The real question is not
   *"what number stops `dlPFC` saturating"* but **"should an E-I loop's inhibition scale with drive, and does
   ours?"** *(`dlPFC` has nine excitatory afferents; `dACC` has few; they share a band. That is the question,
   not the symptom.)*
4. Registered and waiting: `noci → PBN` · the opioid system · the tonic-class sweep.

**`files.zip`: look inside, then un-track.** It is a 495 K binary in a **public** repo — thirty seconds now
against discovering later what is in it. **Keep the docs.**
