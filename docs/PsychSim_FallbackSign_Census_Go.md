# Open the fallback-sign census — **as a LOAD-BEARING census, not a list**

**Right call to ask. And yes — this is the one piece of queued work that does not wait on me, because it needs
literature to FIX and not to RUN. Report only. Nothing builds.**

---

## 1. Why this one, and why now

**After Q1 I would rather not guess a size again.** The sweep was meant to answer *"which pacemakers are
scaffold?"* and instead found that **the entire baseline layer is one number** — a fact nobody predicted,
sitting in plain sight in a field we had all read. **The fallback-sign register is in exactly that position
now: 161 edges, a number we have repeated for two weeks without ever asking what is inside it.**

## 2. ★ The framing — this is what makes it worth a pass

**A list of 161 ungrounded signs is not actionable; 161 is too many to ground.** **But the census's real
question is much sharper, and we earned it twice this branch:**

> **`MeA → VMH`'s typing order gave the WRONG sign — and the aggression keystone was GREEN over it.**
> **`VMH → vlPAG`'s typing order gave the RIGHT sign — and nothing distinguished the two from the outside.**

> ### **A fallback-signed edge that is LOAD-BEARING ON A GREEN TEST is the worst object in the substrate: a
> ### passing test is certifying an outcome that rests on a sign nobody chose.**

**That is the v9 lesson at scale — *a passing keystone certifies the OUTCOME, not the mechanism* — and it is
now a query, not an aphorism.** **The census's output is not a list. It is a PRIORITY ORDER: the edges where
we are currently being told we are right by a test that cannot know.**

## 3. Scope it — S44 already narrows this a long way

**The typing-order hazard applies ONLY to multi-transmitter sources.** For a single-transmitter glutamatergic
source, the fallback returns +1 and +1 is correct — **ungrounded, but not hazardous.** **The dangerous set is
the edges leaving the 16 two-transmitter sources, and the six you already flagged: `MeA`, `VMH`, `MPOA`,
`BNST`, `HYPdm`, `PVN`.** **Start there. That is tens of edges, not 161.**

**Per edge, three columns:**
1. **Fallback-signed?** (`dominant_receptor: None`) — mechanical.
2. **Which sign does the typing order currently give, and what does the source's principal transmitter
   actually project with?** — mechanical for the first half, flags the second for me.
3. **★ LOAD-BEARING?** — silence it and run the suite. **Does a GREEN test change colour?** **That column is
   the whole point, and it is the one only you can produce.**

**Three outcomes, all worth having:**
- **fallback + load-bearing on a green** → **the priority list. Top of every grounding pass.**
- **fallback + inert** → **a hollow set member** — the `AFFECTIVE_EMPATHY` / `HYPdm` shape, found from a third
  independent direction.
- **fallback + load-bearing on a red** → cheap; the red already names it.

## 4. Constraints
- **Report only. No re-signing, no re-banding, no "while I'm here."** **Every sign that needs changing needs a
  citation, and citations are mine.**
- **`MeA → BNST` is already known to be unfixable at any single sign** (MePV→postero-intermediate BST and
  MePD→postero-medial BST are two segregated projections inside one edge). **Expect more of that shape — a
  lump projecting to a lump — and record them as SPLIT-BLOCKED rather than forcing a sign.**
- **If an edge is load-bearing on a green and you cannot tell which way it should point: leave it, flag it,
  and I will fetch it.** **Do not resolve it by which way keeps the green.**

## 5. What it feeds
**This IS a chunk of the 209-edge audit, done early because the branch forced it.** And it is the fourth
independent direction pointing at the same defect: **the receptor-sign convention (which found the lumps), the
read-out audit (which found them again), Q1's baseline sweep, and now this.** **When it lands we will know
whether the audit's remaining shape is tens of edges or hundreds — which is the number Ralph actually needs
for the thesis's timeline.**

**Meanwhile the four-pacemaker table is mine.** Call for it whenever; it does not block this.

**Everything holds: `DRN` 0.05 `UNGROUNDED — pending` · keystone green and annotated · freezing floor the one
authorized red at four terms · `DRN→vlPAG`, `VMH→vlPAG`, `DRN→VMHvl`, the attack drive untouched.**
