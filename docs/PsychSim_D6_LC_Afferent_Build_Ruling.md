# D6 #2 door 1 — the LC afferent build: **RULING**

**Verified against the remote:**
- **`_TEMPERAMENT_DOMAIN` = {THREAT, ANXIETY → `defensive_threat`; SEEKING → `reward_approach`; CARE,
  SOCIAL_LOSS → `affiliation`; CONTROL, INSTRUMENTAL_CONTROL → `executive`}. `interoception_autonomic` is NOT
  in it.** ✅ **Your domain call is right and door 3 is not widened.** That was the claim the whole build hung
  on and it holds.
- **`CeA → LC` is `moderate`.** ✅ Your correction, my error — **the sixth instance of the same pattern.** The
  fix scopes to the real band.

---

## 1. Your point 1 is the better half of my spec — and it resolves two registered items

> *"`PGi` must itself be afferented or the fix just moves the incompleteness one synapse rostral."*

**That is the failure mode applied to the fix, before building it — which is what I should have written and
did not.** And the resolution you found is the payoff:

> **`PBN → LC` and `NTS → LC` were never "dormant." They were WRONG-TARGET.** Aston-Jones showed they do not
> project to LC — they **terminate adjacent** — and **`PGi` is their integrator.** So `PBN → PGi`,
> `NTS → PGi`, `nociception → PGi`, `PGi → LC`. **`PGi` is what those candidates were waiting for, and the
> register had them pointed at the wrong node for as long as they have existed.**

**Two registered items resolve, and they go live immediately** — `PGi`'s afferent set is **available now**, so
building it creates no new unafferented hub. **That is the whole justification for building it.**

---

## 2. SCOPE RULING: build `PGi`. **Not `PrH`. Not `preBötC`.** And record the remainder.

### ✅ `PGi` — BUILD
Major excitatory glutamatergic afferent (~73 % of LC neurons driven); integrates exteroceptive + interoceptive
(pain, cardiovascular, respiratory, arousal). **Afferents available now** (`PBN`, `NTS`, `nociception`).
**Domain `interoception_autonomic`** — verified not temperament-reachable. **It alone dissolves the
entailment: with `PGi` carrying the majority, `CeA` is minor by anatomy, and nothing was edited.**

### ⛔ `PrH` — DO NOT BUILD
It **is** a major afferent, and I specced it. **But its own afferents are the vestibular/oculomotor system,
which does not exist in this substrate.** Building it would create **exactly the unafferented hub we are here
to fix — the failure mode, repeated, inside the fix for the failure mode.** **Register it as a named,
grounded absence, blocked on an unbuilt system.**

### ⛔ `preBötC` — DO NOT BUILD, and the reason is not scope
**Yackle 2017's finding is that a SUBPOPULATION — Cdh9/Dbx1 neurons — carries the LC projection.** Adding a
node called `preBötC` would **lump the rhythm generator with the arousal projection: the tenth lump, added
knowingly, in the pass whose entire subject is lumping.** And the substrate has no oscillation, so the rhythm
is not representable regardless. **Register `preBötC-Cdh9` with the respiratory pass — one circuit, two
registers, and it is a subpopulation.**

### ★ 3. And the failure mode's ACTUAL fix — this is the part that matters

**"Partial completion recorded as completion" — the failure was in the RECORDING, not the completion.**
*"LC now has a teaching signal"* was true. *"LC is afferented"* was not. **So the fix is not "complete
everything." It is "never again record a hub as complete when it is not."**

**So: build `PGi`, dissolve the entailment, and RECORD `LC`'s afferent set as INCOMPLETE, with the remainder
named:** `PrH` (blocked on the vestibular system) · `preBötC-Cdh9` (with the respiratory pass) ·
Barrington's nucleus · the PVN dorsal cap · spinal lamina X.
**That is the honest state — and writing it is precisely what we failed to do the first time.**

---

## 4. ★ An audit gap nobody found — including me

Reading `_TEMPERAMENT_DOMAIN` to verify your domain call, I found its comment:

> *"FRUSTRATION maps to the attack circuits, **which are net-inhibited in v8 (OBS-3)**, so it has no leverage
> here."*

**`FRUSTRATION` is excluded from the temperament map because the attack circuits were net-inhibited. The PAG
split destroyed that premise** — the v9 keystone flip found that suppression was **column confusion**: `CeA`
inhibits **freezing**, not attack. **So the exclusion's justification is stale, and `FRUSTRATION` may now have
leverage. Nobody has checked, and the D6 audit could not have caught it, because it looked at the sets.**

> **THE GAP, generalised — add it to the audit: we enumerated what is IN the sets. We never checked whether
> the reasons for what is OUT are still true. AN EXCLUSION IS A CLAIM, and it goes stale like any other —
> especially one justified by a substrate fact, in a substrate we are changing every commit.**
> **Every exclusion must carry a justification, and every justification must be re-checked when the thing it
> rests on moves.** *(This is the same shape as my own six stale-memory errors: a fact recorded once, true
> then, false now, and nobody re-read it.)*

**Diagnose `FRUSTRATION` in its own small pass. Do not act on it here.**

---

## 5. Build, then measure
`PGi` (+ `PBN→PGi`, `NTS→PGi`, `nociception→PGi`, `PGi→LC`) · re-mark the mis-targeted `PBN→LC`/`NTS→LC`
register entries as **WRONG-TARGET, resolved** · **`CeA → LC` untouched at `moderate`** · `LC`'s set recorded
**INCOMPLETE** with the remainder named.

**Then re-measure: throttle `AFFECTIVE_EMPATHY` → does `LC` still collapse (54 % rest / 72 % threat / 73 %
distress)?** **Either answer is worth having. Neither is engineered.**

**⛔ And door 1 only. #2 stays OPEN.** Door 3 (the dial scales `LC`'s node directly — a CU agent is still born
with a throttled teaching signal) and door 2 (the scan ranges over its own signature) are untouched. **You
have held that line correctly twice now; hold it through the measurement, when door 1's numbers will look
like a resolution.**
