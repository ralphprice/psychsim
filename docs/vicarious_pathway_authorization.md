# Vicarious pathway — DIAGNOSIS + CANDIDATE-EDGE LIST for authorization (no connectome write yet)

Submitted for authorization before any edge is written (Allen-audit discipline: existence/direction + grounding
per edge, weights as scaffold). Includes the refined 3-part re-test criterion and the pre-registered prediction,
both recorded BEFORE the build per ruling.

## Diagnosis — the gap is narrow downstream, but there are TWO unfed routes to aIns

The cortical social-perception chain is almost entirely wired:
`RET→V1→V-dorsal/V-ventral→pSTS→rSMG-TPJ→aIns→dACC`, and `V-ventral→MeA` directly. The shared-affect
destination (aIns) and its ACC-side affective target (dACC, via `aIns→dACC`) both exist.

**Route A — WITNESSED-DISTRESS (the ruled vicarious pathway) is unfed at its INPUT.** The distress-perception
channels reach only the subcortical aversive route: `biological_motion→SC-Pv`, `face_like→SC-Pv`,
`voice→A1-belt→LA`. None enters the cortical stream that reaches pSTS. So social perception never reaches the
shared-affect node.

**Route B — OWN-PAIN AFFECTIVE (discovered during diagnosis; surfaced for a scope ruling) is ALSO unfed.** The
Craig interoceptive pathway `PBN→VMpo-thal→pIns→mIns→aIns` exists, but `nociception` does not feed it —
nociception goes to `CEl, LA, PGi, VPL` only; `PBN`'s afferents are `NTS, SOM-relay, aIns` (no nociceptive arm).
So aIns codes neither own-pain (Route B dark) nor witnessed-pain (Route A dark) — which is why it read +0.008 /
+0.0005 to everything in the CEl-discrimination test. **The shared-representation account requires aIns to code
BOTH** (that is what "shared" means; Singer 2004). This is distinct from the S55/D6 `noci→PBN` question, which
concerned the PGi/LC AROUSAL arm and was correctly closed; the AFFECTIVE VMpo arm is a different pathway.

## Candidate-edge list — CORRECTED per ruling (hierarchy-respecting circuit edges; Route B break traced).

ADD, never replace; grounded edge-by-edge, independent of the construct. `SC-Pv` = "Superior colliculus ->
pulvinar" (a colliculo-pulvinar node), so routing its existing channel landings up to cortex IS the pulvinar->
extrastriate projection -- faithful, not a channel->mid-cortex bypass. Both `biological_motion` and `face_like`
already land on `SC-Pv`; `voice` already lands on `A1-belt`; `nociception` already lands on LA/CEl/VPL/PGi.

| # | edge | grounding (construct-independent) | scaffold weight |
|---|---|---|---|
| A1 | `SC-Pv → V-dorsal` | pulvinar->MT/dorsal-stream projection; the subcortical biological-motion "low road" to MT (Vuilleumier; Grossman & Blake). Carries biomotion (already on SC-Pv) into `V-dorsal→pSTS` (exists). | moderate |
| A2 | `SC-Pv → V-ventral` | pulvinar->ventral-extrastriate projection; the subcortical face route to cortex. Carries face_like (already on SC-Pv) into `V-ventral→pSTS` and `V-ventral→MeA` (both exist). | moderate |
| A3 | `A1-belt → pSTS` | the temporal voice area lies in/near STS and receives from auditory belt (Belin et al. 2000). `voice→A1-belt` already lands the channel; this connects it onward. | moderate |
| B1 | `IN-SOMATO:nociception → VMpo-thal` | lamina-I spinothalamic nociceptive projection to VMpo, the interoceptive/affective pain thalamus (Craig 2002). This is the TRACED break: `VMpo-thal`("Interoceptive thalamus") is fed only by PBN, PBN by `SOM-relay`(touch)/NTS(intero) -- so NOTHING noxious reaches the affective interoceptive route. NOT `->PBN` (PBN is already fed and `PBN→VMpo-thal` exists -- that would duplicate and reopen the closed S55/D6 arousal item). Downstream `VMpo→pIns→mIns→aIns` intact. | moderate |

Downstream requires NO new edges. Existing chains: `V-dorsal/V-ventral→pSTS→rSMG-TPJ→aIns→dACC`,
`V-ventral→MeA`, `VMpo-thal→pIns→mIns→aIns`. Subcortical `→CEl/dPAG` (SC-Pv), `→LA` (A1-belt) and
nociception's amygdala/VPL/PGi edges are all KEPT -- every addition is additive.

**Anatomical constraints honoured (ruled):** the witnessed-distress signal reaches the AFFECTIVE nodes
(aIns, dACC via `aIns→dACC`), NOT through somatosensory relays -- no S1/S2/VPL edge proposed. The own-pain
signal (B1) enters the AFFECTIVE interoceptive thalamus (VMpo), NOT the sensory-discriminative VPL route.
`dACC` is the ACC-side affective node; no `rACC`/`pgACC` node exists -- flagged, not built.

## Why Route B is REQUIRED (ruling, stronger than the hollow-specificity argument)

Route A alone would build a node (`aIns`) that responds ONLY to another's distress -- a dedicated empathy
detector, a mechanism that contains the answer. The vicarious response BORROWS the first-person affective
representation; without the first-person route (B1) there is nothing to borrow. So B1 is what makes A honest,
not scope creep. Authorised in principle; the corrected edge is `nociception→VMpo-thal` (traced), not `->PBN`.

## Refined re-test criterion (RECORDED before build; refined on grounded grounds, per ruling)

The original own-pain/distress ratio threshold is the WRONG validator for a correctly-built pathway, because
shared affective representation means aIns is activated by own-pain too (own > other in magnitude is NORMAL). A
criterion that fails a pathway for showing own-pain activation would reject the correct anatomy. Refined:

1. **PRIMARY — reach & response:** does the conspecific-distress cue now produce a specific, substantial
   response in the shared-affect nodes (`aIns`, `MeA`), which currently read +0.0005 / +0.0000? The pathway is
   functional iff a route now exists and carries signal. This is the direct test of the missing mechanism.
2. **SECONDARY — ratio vs the recorded 6.6× baseline:** report the new own-pain/distress ratio against 6.6×,
   interpreted against the shared-representation expectation (own > other is normal), NOT as pass/fail on the
   old threshold. If the ratio barely moves despite the pathway existing, that is a finding.
3. **SPECIFICITY — moved to MeA (RULED), recorded before build with reasoning.** With Route B built, own-pain
   SHOULD reach `aIns` (that is the point; by Singer's account own-pain is often the LARGER response), so
   "distress > non-social aversive at aIns" would be ambiguous and could reject the correct anatomy. `MeA` is
   conspecific-specific BY CONSTRUCTION (afferents `PVN-OT`/`PIR`/`V-ventral` -- social/olfactory/oxytocin, no
   nociceptive afferent), so own-pain should NOT reach it. CRITERION: the distress cue activates `MeA` (via
   A2: face_like→SC-Pv→V-ventral→MeA) while a matched non-social aversive cue does NOT. This is a clean
   specificity probe that survives both routes being built. (`aIns` keeps criteria 1 and 2.)

## Pre-registered prediction (before build; failure is a live possibility, per the anti-fitting guard)

- PRIMARY: I predict the distress cue WILL now reach `aIns` (via A1/A2→pSTS→rSMG-TPJ→aIns) and `MeA` (via
  A2→V-ventral→MeA) and elevate them meaningfully (>~0.05 over baseline, vs the current +0.0005/+0.0000). LIVE
  POSSIBILITY it stays weak: the multi-hop cortical route may attenuate through the settle.
- SECONDARY (aIns ratio): I predict the ratio drops from 6.6× but stays >1 (own > other is normal now that B1
  gives aIns own-pain via VMpo). Will NOT pass/fail on the old threshold.
- SPECIFICITY (MeA): I predict the distress cue activates `MeA` while a matched non-social aversive cue
  (nociception/bitter/CO2) does not -- nociception now reaches aIns (via VMpo) but has no path to MeA. A clean
  pass here is the genuine test (no longer hollow, because B1 is built).
- OVERALL failure possibility (recorded): a correctly-grounded pathway might still not produce a clean empathy
  signal if the shared-affect response is swamped by the aversive fan-out, or if MeA does not respond to the
  face route. If so, THAT is the next finding, not a licence to tune weights. Weights are scaffold-then-grounded;
  they will NOT be adjusted to move any number.

## Anti-fitting commitments (binding)

- Each edge (A1–A3, and B if authorized) is grounded on anatomy, defensible to someone who has never seen the
  contamination result. None references what it does to the construct.
- Weights are scaffold; if the grounded pathway does not restore the construct, that is reported as a finding.
- After build: regrow (connectome change) + full-suite gate → re-derive the affective-empathy read-out on the
  nodes that MEASURABLY carry the vicarious signal → re-run CEl-discrimination against the 3-part criterion. The
  construct returns ONLY if it passes. (Binding.)
