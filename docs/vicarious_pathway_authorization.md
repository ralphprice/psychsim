# Vicarious pathway — DIAGNOSIS + CANDIDATE-EDGE LIST for authorization (no connectome write yet)

Submitted for authorization before any edge is written (Allen-audit discipline: existence/direction + grounding
per edge, weights as scaffold). Includes the refined 3-part re-test criterion and the pre-registered prediction,
both recorded BEFORE the build per ruling.

## Diagnosis — the gap is narrow downstream, but there are TWO unfed routes to aIns

The cortical social-perception chain is almost entirely wired:
`RET→V1→V-dorsal/V-ventral→pSTS→rSMG-TPJ→aIns→dACC`, and `V-ventral→MeA` directly. The shared-affect
destination (aIns) and its ACC-side affective target (dACC, via `aIns→dACC`) both exist.

**Route A — WITNESSED-DISTRESS (the ruled vicarious pathway) is unfed at its INPUT.** The distress-perception
channels reach only the subcortical aversive route: `biological_motion→SC-Pv`, `face_like→SC-Pv`,
`voice→A1-belt→LA`. None enters the cortical stream that reaches pSTS. So social perception never reaches the
shared-affect node.

**Route B — OWN-PAIN AFFECTIVE (discovered during diagnosis; surfaced for a scope ruling) is ALSO unfed.** The
Craig interoceptive pathway `PBN→VMpo-thal→pIns→mIns→aIns` exists, but `nociception` does not feed it —
nociception goes to `CEl, LA, PGi, VPL` only; `PBN`'s afferents are `NTS, SOM-relay, aIns` (no nociceptive arm).
So aIns codes neither own-pain (Route B dark) nor witnessed-pain (Route A dark) — which is why it read +0.008 /
+0.0005 to everything in the CEl-discrimination test. **The shared-representation account requires aIns to code
BOTH** (that is what "shared" means; Singer 2004). This is distinct from the S55/D6 `noci→PBN` question, which
concerned the PGi/LC AROUSAL arm and was correctly closed; the AFFECTIVE VMpo arm is a different pathway.

## Candidate-edge list — ROUTE A (the ruled vicarious pathway). ADD, never replace. Grounded edge-by-edge.

| # | edge | grounding (independent of the construct) | scaffold weight |
|---|---|---|---|
| A1 | `IN-VIS:biological_motion → V-dorsal` | biological-motion perception engages the dorsal/MT visual stream projecting to pSTS (Grossman & Blake 2002; Puce & Perrett 2003). `V-dorsal→pSTS` already exists. | moderate |
| A2 | `IN-VIS:face_like → V-ventral` | face processing engages the ventral/fusiform (FFA) stream, which projects to STS and amygdala. `V-ventral→pSTS` and `V-ventral→MeA` already exist (Haxby et al. 2000). | moderate |
| A3 | `IN-AUD:voice → pSTS` | the temporal voice area lies in/near STS and receives from auditory belt; voice-selective STS (Belin et al. 2000). (`voice→A1-belt` already exists; this adds the belt→STS social arm. Could equivalently be `A1-belt→pSTS`.) | moderate |

Downstream requires NO new edges: A1/A2/A3 → the existing `pSTS→rSMG-TPJ→aIns→dACC` and `V-ventral→MeA` chains.
The subcortical `→SC-Pv` orienting edges are KEPT (the "low road" is real); Route A is additive.

**Anatomical constraint honoured (ruled): the vicarious signal reaches the AFFECTIVE nodes (aIns, dACC) via
pSTS→rSMG-TPJ, NOT through somatosensory relays (S1/S2/VPL).** No S1/S2 edge is proposed. dACC is the ACC-side
affective node (already `aIns→dACC`); the model has no `rACC`/`pgACC` node — whether a dedicated one is owed is
flagged, NOT built here.

## ★ Route B — surfaced for a SCOPE ruling (NOT proposed for build without authorization)

If the specificity criterion is to be meaningful, aIns should code own-pain too (shared representation), which
needs the affective own-pain arm fed: candidate `IN-SOMATO:nociception → PBN` (or `→ VMpo-thal`), grounding =
the lamina-I spinoparabrachial/spinothalamic nociceptive projection to the affective interoceptive pathway
(Craig 2002). **Question for ruling: is Route B in scope for this pass, or a separate pain-affect pass?**
Consequence either way is recorded in the criterion below.

## Refined re-test criterion (RECORDED before build; refined on grounded grounds, per ruling)

The original own-pain/distress ratio threshold is the WRONG validator for a correctly-built pathway, because
shared affective representation means aIns is activated by own-pain too (own > other in magnitude is NORMAL). A
criterion that fails a pathway for showing own-pain activation would reject the correct anatomy. Refined:

1. **PRIMARY — reach & response:** does the conspecific-distress cue now produce a specific, substantial
   response in the shared-affect nodes (`aIns`, `MeA`), which currently read +0.0005 / +0.0000? The pathway is
   functional iff a route now exists and carries signal. This is the direct test of the missing mechanism.
2. **SECONDARY — ratio vs the recorded 6.6× baseline:** report the new own-pain/distress ratio against 6.6×,
   interpreted against the shared-representation expectation (own > other is normal), NOT as pass/fail on the
   old threshold. If the ratio barely moves despite the pathway existing, that is a finding.
3. **SPECIFICITY:** the distress cue must activate `aIns`/`MeA` MORE than a non-social aversive cue of
   comparable intensity. This distinguishes a vicarious pathway from a wider aversive fan-out. NOTE: if Route B
   is NOT built, this passes trivially (own-pain can't reach aIns at all), which is a HOLLOW pass — flagged, and
   the reason the Route B scope question matters.

## Pre-registered prediction (before build; failure is a live possibility, per the anti-fitting guard)

- PRIMARY: I predict the distress cue WILL now reach `aIns`/`MeA` and elevate them meaningfully (>~0.05 over
  baseline), via A1/A2 → pSTS → rSMG-TPJ → aIns and A2 → V-ventral → MeA. LIVE POSSIBILITY it stays weak: the
  multi-hop cortical route may attenuate, or the amygdala aversive fan-out may still swamp the read-out.
- SECONDARY: I predict the ratio drops from 6.6× but stays >1 (own > other, normal). Will not pass/fail on it.
- SPECIFICITY: with Route A only, I predict distress ≫ nociception at aIns (own-pain route still dark) — a
  hollow-but-true pass; with Route B added, a genuine test. This is exactly why the Route B scope question is
  raised BEFORE building.
- OVERALL failure possibility (recorded): a correctly-grounded pathway might still not produce a clean empathy
  signal if the shared-affect response is swamped by the aversive fan-out. If so, THAT is the next finding, not
  a licence to tune weights. Weights are scaffold-then-grounded; they will NOT be adjusted to move the ratio.

## Anti-fitting commitments (binding)

- Each edge (A1–A3, and B if authorized) is grounded on anatomy, defensible to someone who has never seen the
  contamination result. None references what it does to the construct.
- Weights are scaffold; if the grounded pathway does not restore the construct, that is reported as a finding.
- After build: regrow (connectome change) + full-suite gate → re-derive the affective-empathy read-out on the
  nodes that MEASURABLY carry the vicarious signal → re-run CEl-discrimination against the 3-part criterion. The
  construct returns ONLY if it passes. (Binding.)
