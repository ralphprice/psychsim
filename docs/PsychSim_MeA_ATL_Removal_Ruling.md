# `MeA → ATL-TP` — **REMOVE.** And what the test proved, stated precisely.

**The silencing test is decisive: all four labels unchanged, the classification holds entirely on
`BA → ATL-TP`. The edge is inert, `basis: assumption`, and anatomically the wrong source. Remove it. This is
the `SC-Pv → PGi` shape resolved by deletion instead of refusal — an assumed edge that was doing a grounded
edge's work, found because we asked whether it should exist rather than which way it should point.**

**Subject to the standing rule: this needs Ralph's explicit go, because it is a connectome change. I am
ruling FOR removal; Ralph clears it.**

---

## 1. Before it comes out — state what the census actually proved, because it is the payoff and it is easy to
underweight

**The census was opened to size a two-week-old number (161 fallback edges). It resolved to one edge under
pressure. And that one edge turned out to be:**
- the single fallback-signed edge that was **load-bearing on a meaningful green**, AND
- the single **`assumption`-based** `MeA` efferent, AND
- carrying a **thesis classification label**, AND
- **inert once corrected** — the hazard was the WRONG sign, not the edge's existence.

> **Four independent properties, and they converged on ONE edge out of 240.** **That convergence is not luck —
> it is what a real audit produces: the worst object in the substrate is worst along several axes at once,
> because the same underlying defect (an assumed edge from a lump, never grounded, never tested) shows up as a
> sign hazard AND a provenance gap AND a load-bearing thesis dependency simultaneously.** **The census found
> the intersection, and the intersection was a single edge.**

**That is the sentence for Ralph: the 209-edge audit's fallback-sign component, sized honestly, was one edge
to fix under pressure — and the reason it was only one is that the hazardous, the ungrounded, and the
load-bearing all pointed at the same place.**

## 2. ★ And the discipline held at the substrate's single worst point — this is the real result

**Consider what did NOT happen.** The correct sign (−1) is inert; the wrong sign (+1) would have flipped a CU
classification label. **A model built to produce a wanted classification would have had +1 there — it makes
the label "more interesting," it is defensible-sounding (amygdala excites cortex), and nothing but the
receptor biology says otherwise.** **We had −1, by accident, and when we checked, the biology confirmed −1 and
then told us the edge should not exist at all.**

> **The honest wall held at the exact point where a thesis output met an ungrounded sign — the highest-pressure
> point in the whole substrate. It held not because we were careful there, but because the discipline is
> structural: fallback typing is blind to what we want, the census tests load-bearing mechanically, and the
> existence test runs on data. Not one of those three steps consulted the desired classification.** **That is
> the strongest single piece of evidence this branch has produced that the model is not being fitted to its
> conclusions — and it lands on the CU study's actual output.**

---

## 3. Removal mechanics — do it deliberately, not as a footnote

1. **Remove `MeA → ATL-TP` from the connectome.** 240 → 239 edges.
2. **★ This changes the characterisation golden** — `ATL-TP`'s afferent set changes even though its output
   label does not, and the golden is 4-decimal exact (your hair-trigger finding). **Regenerate it as a
   DELIBERATE, DOCUMENTED regeneration** — the same standard as every prior golden shift: state the reason
   (removal of an assumption-based inert edge), confirm ONLY `ATL-TP`-adjacent values move, spot-check the
   four labels are byte-identical. **A silent golden regen on a connectome deletion is how a real change hides
   in a demonstrated-reasons-only history.**
3. **Record it as the project's FIRST connectome deletion** — every prior change added or re-signed; nothing
   has been removed. **The precedent matters: an edge earns removal by being (a) inert on measurement AND (b)
   ungrounded in provenance. Both required.** **`MeA → ATL-TP` is the template — an assumed edge, proven
   load-free, deleted with the golden regenerated.** *(An inert but GROUNDED edge stays — absence of current
   effect is not absence of anatomy. A load-bearing but assumed edge gets escalated, not deleted. Only the
   intersection — inert AND assumed — earns the delete.)*
4. **The commit message carries the citation for the deletion** the way the others carry it for additions:
   the silencing result (labels invariant) + the provenance (`basis: assumption`) + the anatomy (TP–amygdala
   is basolateral, `BA → ATL-TP` is the grounded source).

---

## 4. It is one instance of a class — flag it, do not chase it now

**`MeA → ATL-TP` was assumption-based. It is almost certainly not the only one.** **The census found it
because it was ALSO fallback-signed and load-bearing — but an assumption-based edge that is correctly signed
and inert would NOT have surfaced.** **There is a separate, un-run query hiding here:**

> **How many edges are `basis: assumption` at all?** That is a one-line seed query, mechanical, and it is the
> provenance sibling of the fallback-sign census. **It does not need to run now** — but register it, because
> the 209-edge audit will need it, and it may reframe "grounded connectome" the way Q1 reframed "grounded
> baselines." **Do not open it this pass; the removal is the actionable item.**

---

## 5. RULING
- **Remove `MeA → ATL-TP`** (Ralph clears). Golden regenerated deliberately, documented, `ATL-TP`-adjacent
  only, labels invariant.
- **Record the removal precedent**: inert-AND-assumed earns deletion; inert-alone or assumed-alone does not.
- **Register the `basis: assumption` census** as the fallback-sign census's provenance sibling — do not run
  it this pass.
- **S63 stands and gets its sharper form**: the substrate's single worst object was worst along four axes at
  once, the discipline held there, and it was one edge.

**The two `LA` receptor pins remain cheap grounding on your word. The four-pacemaker table is mine to run when
you call. `DRN` 0.05 `UNGROUNDED — pending`; keystone green; freezing floor the one authorized red, four
terms.**
