# The CeL gap the SOM+ removal revealed — RULING. **Add the CRF+ flight population. It's the missing half of
# the competing pair, and it completes Lump #13.**

**The freezing column is alive and correctly grounded, and the SOM+ removal revealed a real gap rather than
causing one — the diagnosis is right, and the literature confirms it decisively. The gap is the missing half of
the actual grounded competing pair. Rulings.**

---

## 1. The freezing fix is grounded and correct (confirmed)

**`PL → vlPAG` landed and the freezing column is alive** (vlPAG 0.000 → 0.043 under threat; three gate
failures cleared). And the sign question resolved exactly as flagged:

> **You grounded the sign and it ruled OUT the naive CeM route.** The `CEm-freeze → vlPAG-GABA` (Tovote 2016)
> pathway was already built and measured delivering nothing (the gate sits at 0.000 under threat); a direct
> `CEm-freeze → vlPAG` would have to be INHIBITORY (CeM is GABAergic per the S59 census) and would have
> suppressed the column. **So the column needed excitation, and PL is where the literature puts it (prelimbic
> = fear expression). The conditional applied exactly, and grounding the sign is what prevented a wrong-signed
> edge.** This is the honesty wall operating on a sign — the naive route was anatomically plausible and
> measurement-plus-grounding ruled it out.

**And it corrected a real defect:** `dmPFC`'s only prior route to the column was onto `vlPAG-GABA` — the fear-
EXPRESSION node was driving the freezing BRAKE, backwards for prelimbic. **The fix surfaced and corrected an
inverted pathway.** Good.

## 2. ★ The revealed gap — the literature confirms it, and names the missing population

**The SOM+ removal revealed that the active arm has no selector gate: `CEm-active` tracks BA drive regardless
of which population wins (0.272/0.273/0.274 across affiliation at threat 1.0). The diagnosis: the model
conflated two literatures. I verified against Fadok 2017 (the primary source), and it is decisive:**

> **The central amygdala selects active vs passive defence via a COMPETITIVE INHIBITORY CIRCUIT of two mutually-
> inhibitory CeL populations: CRF+ (mediates conditioned FLIGHT) and SOM+ (initiates passive FREEZING)** (Fadok
> et al., Nature 2017, "A competitive inhibitory circuit for selection of active and passive fear responses").
> **And CRF+ neurons project to VMH and "widely throughout the PAG columns" including the dorsolateral/lateral
> PAG** (the flight/escape columns). SOM+ initiates freezing; CRF+ drives flight; they mutually inhibit.

**★ The model conflated TWO DISTINCT AXES:**
- **Haubensak's PKCδ+** gates the CeM output — a GATING axis (correctly grounded, retained: `CEl-PKCδ →
  CEm-freeze`).
- **Fadok's CRF+ vs SOM+** selects active vs passive behaviour — a SELECTION axis (the competing pair).

**These are different distinctions doing different jobs, and the model had only SOM+ (plus PKCδ+), with the
removed `SOM+ → CEm-active` edge standing in for the missing CRF+ population.** So removing the refuted edge
revealed the gap: **there is no CRF+ flight-driver population, so the active arm cannot be selected — it just
follows BA drive.** This is the lumping pattern one level further in — **the same defect that opened Lump #13:
two distinct CeL populations (the CRF+/SOM+ competing pair) doing one job under a partial naming.**

## 3. RULING — add the grounded CRF+ CeL population; it completes the competing pair and closes Lump #13

**Add the CRF+ CeL population as the flight/active-defence driver, mutually inhibitory with SOM+ (the Fadok
competitive circuit), projecting to the active-defence targets.** This is grounded (Fadok 2017 is decisive and
directly cited) and it is the honest completion of Lump #13 — the un-lumping revealed that CeL selection
requires the CRF+/SOM+ competing pair, not a single SOM+ with an inferred output edge.

- **CRF+ population:** the flight/active-defence driver. Mutually inhibitory with SOM+ (SOM+ ⊣ CRF+ and
  CRF+ ⊣ SOM+ — the competitive circuit). **This is the selection axis: which population wins determines
  active (CRF+) vs passive (SOM+) defence.**
- **CRF+ projections:** to the active-defence / escape targets — VMH and the dorsolateral/lateral-PAG-equivalent
  (the model's `dPAG`, the flight/attack column), grounded in Fadok's CRF+ → VMH and CRF+ → PAG-columns finding.
  **This is what drives `CEm-active` / the aggression-flight output when CRF+ wins — giving the active arm the
  selector gate it currently lacks.**
- **The mutual inhibition is the selector:** CRF+ vs SOM+ compete, the drives determine which wins, and the
  winner drives its output (CRF+ → active/flight, SOM+ → freezing). **The selection EMERGES from drive balance
  — exactly the emergence the exit measurement requires, now with the correct grounded populations.**

**Sign/receptor:** CeL populations are GABAergic (both CRF+ and SOM+ are inhibitory CeL neurons); the mutual
inhibition is GABAergic; ground the projections' signs carefully (CRF+ → VMH/PAG may be disinhibitory or via a
specific mechanism, as with the freeze arm — the diagnosis-then-ground discipline applies to each new edge).

**★ Caveat to register:** Fadok 2017 reports CRF+/SOM+ as mutually EXCLUSIVE, but later work (McCullough 2018,
Hartley 2019) shows some CRF/SOM overlap in CeL. **Ground the populations as the Fadok competitive pair — the
cleanest, most-cited, and behaviourally-decisive model — and register the overlap as a known simplification**
(the two populations are modelled as distinct because that is the competitive-selection mechanism; the partial
overlap is a fidelity item, not this pass). This is the same "the competing-pair model is the grounded
mechanism; the overlap is a registered simplification" discipline.

## 4. What this does NOT do — the refuted edge stays removed

**Do NOT restore `SOM+ → CEm-active`.** It was refuted (Fadok: SOM+ acts by local inhibition, not a CeM output
projection) and the S56 exit is explicitly NOT closeable by restoring it. **The active arm gets its selector
from the CRF+ population winning the mutual inhibition and driving its OWN active-defence targets — not from
SOM+ projecting to the active output.** The grounded fix is the missing population, not the refuted edge.

---

## 5. Handoff

**Add the grounded CRF+ CeL population (Fadok 2017): the flight/active-defence driver, mutually inhibitory with
SOM+ (the competitive selection circuit), projecting to the active-defence targets (VMH, dl/l-PAG-equivalent /
`dPAG`), with signs grounded per-edge. This completes the CeL competing pair (CRF+ flight vs SOM+ freezing) and
closes Lump #13 — the un-lumping's real endpoint was that CeL selection needs BOTH populations, not one SOM+
with an inferred output edge. Register the CRF/SOM overlap (McCullough, Hartley) as a known simplification;
ground the populations as the Fadok competitive pair. Do NOT restore the refuted SOM+→CEm-active edge.**

**Then take the S56 exit measurement WHOLE:** freezing (SOM+ wins the mutual inhibition → drives the freezing
column) and aggression/flight (CRF+ wins → drives the active-defence column) as DISTINCT selector states, the
selection EMERGING from drive balance (perturb-and-follow — perturb the drives, watch which population wins and
which output fires), no scaffold weight the selection rests on. **That measurement closes S56.**

> **The freezing column is alive and correctly grounded — PL→vlPAG, with the CeM GABAergic sign correctly ruling
> out the naive route, and an inverted dmPFC pathway corrected. The SOM+ removal revealed the real gap: the
> model had half of Fadok's competing pair. The un-lumping's true endpoint is the CRF+/SOM+ competitive circuit
> — add the grounded CRF+ flight population, mutually inhibitory with SOM+, driving the active-defence targets,
> and the active arm becomes a selector state the drives choose. That completes Lump #13 with its actual
> grounded mechanism (two populations, not one plus an inferred edge), and the S56 exit measurement — freezing
> and flight/aggression as distinct emergent selector states — becomes takeable whole. The last population is
> the last piece.**
