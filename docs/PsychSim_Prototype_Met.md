# Prototype MET. **One check, then take the floor.**

**Gate 6 clean — 594 tests, 0 fail/error, first fully-green clearance of the corrected tree — and an
end-to-end study run producing graded developmental outcomes on an instrumented, banned-pattern-free study
layer. That is the prototype as defined. Goal met.**

---

## 1. Three things worth naming about how it was reported

**(a) The graded fix argued for itself.** 16 of 20 labels knife-edge (margin < 0.05) means **the bare argmax
would have presented sixteen near-ties as confident categories.** The change was ruled on four accumulated
failures; the prototype run demonstrates the necessity directly, on the study's own output. That is the best
possible justification for a change — the first run under it shows what the old read-out was doing.

**(b) The refusal at the point of maximum temptation.** The psychopathic condition came out uniquely
low-affiliation (0.083, half the others) with elevated reward_approach — **the reward-driven low-affiliative
profile, which is what the dead Buckholtz finding claimed.** It was recorded as a fact of the graded profile and
*not* claimed as confirmation. **That is the contaminated move refused at the exact moment it would have been
easiest and most gratifying to make.** The parked dynamics question stays parked and pre-registered.

**(c) Caveats in the output header, not only the register.** A reader of the output sees that absolute learning
magnitudes are untrustworthy pending the floor. **Provenance travelling with the artifact rather than living in
a separate document is the right default**, and it is what the Stage-3 regen taught.

## 2. One cheap check before moving on

**`fearless` produced a profile identical to `shared_root`.** Two candidates, and they have opposite meanings:
- **The two seeds are genuinely the same gain vector in this harness config** → a harness artifact; label the
  row as such or drop it, and nothing follows.
- **The seeds differ but the profile does not** → the graded profile is insensitive to a temperament difference
  that *does* enter substrate dynamics (`engine.py:196/223` — the throttle multiplies drive propagation). **That
  would matter, because the study's entire design manipulates temperament**, and a profile read-out that cannot
  see a seed difference the dynamics carry is the audit's pattern in a new place.

**One line: are the two seeds' gain vectors different in this config?** If they are, measure why the profile
does not move. Do not build anything on it yet — just discriminate.

## 3. Next — no further ruling needed

**Take the floor grounding.** It is ruled, it is a measurement before it is a decision, and the prototype's own
caveat points at it:
1. Measure the model's adult:developmental learning-rate ratio.
2. Compare to the grounded band (adult ≈ order-of-half the developmental magnitude, not a thousandth).
3. Ground the floor to the ratio.
4. **Re-measure the three near-zero results together** — punishment learning, cue-alone, DA paired gain. **If
   all three move, that co-movement is the confirmation; if they don't, the floor wasn't the cause and each
   needs its own explanation.**

Then the sweeps, the `gaps_register` pass, and the scoped items.

> **Prototype met, on a clean gate, with the graded fix demonstrating its own necessity and the strongest
> temptation of the run correctly refused. Discriminate the identical fearless/shared_root profiles with one
> line — harness artifact or read-out insensitivity, they mean opposite things. Then take the floor grounding:
> measure the ratio, ground it, and watch whether the three near-zero results move together.**
