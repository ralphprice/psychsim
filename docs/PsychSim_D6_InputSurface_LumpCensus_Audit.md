# Two mechanical audits — the input-surface (catalogue vs channels) and the lump census. Report-only.

Both ruled report-only. Both found more than their seed example, and both are mechanical pre-computations of
the 209-edge audit. **Nothing fixed.** First, the verdict the sweep reinstated.

## 0. ★ The split verdict, REINSTATED — visual now MEASURED, not inferred
- **VISUAL: `TRUE BY ANATOMY`, measured.** The escape gate is surmountable at ~SC-Pv 0.7; observation drives
  `SC-Pv` to 0.584 — sub-escape; **and the threshold that produces that is grounded electrophysiology
  (`dPAG-GABA` 6.2/32.2 Hz → 0.19).** So *"observed distress does not reach arousal via the escape column"* is
  a grounded, measured, **emergent** fact — not a gap. `dPAG` is escape-gated, and observation is correctly
  sub-escape; nobody coded that, a grounded rate produced it. (Both my earlier verdicts were inferred, not
  measured; the sweep settled it.)
- **AUDITORY: still live** — the injection depth (§1 below makes it worse, not better).

## 1. Catalogue-vs-channels audit — cited reinforcers that silently cannot fire
14 cited innate reinforcers vs 23 wired input channels. **A missing channel is a NULL, not an error — so each
of these is a mechanism the model has, cites, and cannot exercise:**

| reinforcer | stimulus | channel status |
|---|---|---|
| **`PB-LOOMING`** | looming / impending collision | **NO `IN-VIS:looming`** — and `SC-Pv → dPAG` is `basis: innate_reinforcer` citing it. **The escape TRIGGER. The escape circuit has never been exercised by a real escape cue** (the Half-2 gap, confirmed). |
| **`PB-STARTLE`** | sudden intense sound | **`COCH` is COMPLETELY UNAFFERENTED** (no channel, no circuit) and the only `IN-AUD` is `voice → A1-belt` (cortex). So startle (`COCH → StN → CeA`) is dead at the input. |
| **`PR-SALT`** | salt taste | **NO `IN-GUST:salt`** (sweet/bitter/sour/umami wired; salt not). |
| **`PR-HOMEOSTATIC`** | hunger/satiety/thirst/air-hunger | **partial** — hunger/satiety (`nutritive_state`) + air-hunger (`CO2_acidosis`) wired; **thirst is not.** |
| *(carried)* `IN-PROPRIO` (S23) | proprioception/agency | declared, unwired. |

### ★★ The auditory input surface is BROKEN, and three findings converge on it
`COCH` (cochlear nucleus, the brainstem auditory entry) has **no afferents at all**, and there is **no bare
`IN-AUD`/`IN-AUD → COCH`** — the *only* sound input is `IN-AUD:voice → A1-belt` (cortex). So the entire
**subcortical auditory pathway is unafferented**: `COCH → AUD-brainstem(IC) → SC-Pv/MGN` has no input. This is
one gap explaining **three** prior findings:
1. **Startle** cannot fire (its sensory limb starts at `COCH`).
2. **The cry cannot reach `SC-Pv`** (the auditory-threat path to the SC is unafferented) — the door-1 auditory
   channel.
3. **The cry cannot reach the IC** — Phase C injected `voice` at `A1-belt`, *downstream* of the entire relay,
   and the relay has no other input anyway.

**The auditory input is cortex-only. That is the injection-depth finding generalised: not just "the cry enters
too high" but "the subcortical auditory pathway has no input at all."** Register; do not build (its own pass).

**THE CLASS (S23 generalised): the INPUT SURFACE has gaps, invisible because a missing channel yields a NULL,
not an error. The audit is: every cited innate reinforcer must have a working input channel — and each gap is a
mechanism that silently cannot fire.**

## 2. ★ The lump census — the detector at scale (report-only)
The detector, precise form: **`dominant_receptor: None` AND a source whose transmitter field names TWO
transmitters.** (The raw fallback count is 161/236 = 68%, but the fallback is *correct* for a homogeneous
source — `RET → LGN`, retina glutamatergic, fallback +, right. The two-transmitter filter is what makes it a
lump-detector: the transmitter field naming two is **the lump written into the data**, resolved by coin-flip.)

**16 lump-declaring sources, generated by query — before they break anything:**

**★ Dangerous (subpopulation-lumps — the projection SIGN genuinely flips by subpopulation; the coin-flip can be
backwards, as `MeA → VMH` was):**
`MeA` (MePV glut / MePD GABA — S41) · `VMH` (core glut / shell GABA — S41) · `MPOA` · `BNST` · `HYPdm` · `PVN`
— the **hypothalamic/amygdala** nuclei. These are the wrong-target/missing-element candidates the 209-edge
audit will hit; **the census pre-computes them.**

**Lower-risk (cortical/relay: glutamatergic *projection* + local GABA interneurons; the fallback is probably +
and correct, but the sign is silently order-dependent and must be verified, not assumed):**
`A1-belt` · `AUD-brainstem` · `S1` · `V1` · `VN` · `pIns` · `dlPFC` · `RET` · `NAc-shell` · `PPTg`.

**The finding: 11 lumps were found by accident, one at a time, each because something broke. This detector
finds them by query. Run it before each build; it is the same information the 209-edge audit will pay for,
pre-computed.** And the fallback sign being **order-dependent** on the transmitter field (`"glutamate/GABA"`→ +,
`"GABA/glutamate"`→ −) is itself a hazard: a heterogeneous source's edge sign is set by word order, silently.

## Registered (S42 catalogue-vs-channels, S43 lump census); verdict reinstated (S37). Nothing built. Order:
`MeA`/`VMH` grain (keystone-bound, surface first) → these two audits (done) → `vlPAG`'s drive on the un-lumped
node → door 1's auditory channel (now: the whole subcortical auditory input). **Door 1: visual `TRUE BY
ANATOMY` (measured), auditory live. #2 open on doors 2 and 3.**
