# Time-normalisation landed. **Extend it to the life-loops next — and the reason is a mixed-clock confound, not
# consistency.**

**N-invariance measured (109.1/108.0/107.1 across N=32/64/128, ~2% spread) — my log-integration concern was
worth checking and the answer is that the fix delivers. That is the flag doing its job: it named what to check,
the check ran, and the analysis was not needed. Gate clean, zero flips, every developmental claim re-measured
with attribution.**

**The re-measurement met the standard the ruling set: 48 golden fields moved and all attributable to the
dt-scaled update; the retracted claims stayed retracted (F4 divergence, harsh-mirror sentinel); nothing was
manufactured or rescued. S18 held again — grounding deflated or preserved, never inflated.**

---

## 1. ★ The decisive reason to extend next — a mixed-clock confound in F4

**F3's +3.7291 being byte-identical is the visible symptom. The one underneath it needs checking, and if it
holds it settles the order:**

> **F4 interleaves relational episodes into `develop()`'s childhood via the `on_episode` callback.** `develop()`
> is now time-normalised. **If the relational episodes still accrue `+= 1` per episode, then within a single F4
> run the moral-environment contribution accrues on a TIME clock and the relational contribution accrues on an
> EPISODE clock.** Their relative magnitudes would then be set by the mismatch between two clocks rather than
> by anything grounded.

**That is worse than either pure state.** Before the fix both paths were `+= 1` — commensurable, if arbitrary.
**Now they may be incommensurable, and F4's whole design rests on the two arms differing only in relational
content.** A relational contribution scaled by a different clock than the moral content it is being added to is
not "only relational content."

**RULING: verify this first — does the relational path supply `dt_years`, or does it default to `+= 1`? If it
defaults, extending time-normalisation to the arena/life-loops is the immediate next item, not one of several
follow-ups.** *(Stated as a check, not an assertion — my record on confident mechanism stories is six for six
the wrong way. But it is cheap and it determines the order.)*

**Nothing live currently depends on F4** (its divergence claim is retracted and xfail), **so this is not urgent
in the sense of a wrong claim in the record.** It is urgent in the sense that leaving the system half-normalised
means the next relational measurement is taken on a mixed-clock instrument.

## 2. F3 is a live claim and its magnitude will move

**`+3.7291`, "all 3 agents drift more, 3/3" is a live relational claim, and it is currently measured on the
un-normalised arena — so its magnitude is still episode-count-dependent, exactly the defect just fixed for
`develop()`.**

**When the arena is normalised, re-measure it.** S18 says expect deflation; measure rather than assume. **The
directional claim (3/3 drift more) may well survive — it is ordinal, not a magnitude — while the +3.73 figure
moves. Report both: which survives and at what corrected magnitude.** Same discipline as the emergent-findings
re-verification.

## 3. Order

1. **Verify the F4 clock question**, then **extend time-normalisation to the arena / daily / townlife loops** —
   finishing the job on the paths that carry every relational claim (F1–F4, the harsh mirror, the prototype's
   relational conditions).
2. **Re-measure F3** and any other relational magnitudes on the normalised loops.
3. **Then the integrity sweeps** (5 vacuous + 3 brittle) and the **`gaps_register` structural pass** — batched,
   one gate, neither changing dynamics.
4. Then Route 3, three-store, CEl-discrimination, and the parked Buckholtz question.

---

> **The fix landed and delivered measured N-invariance, the re-measurement met the attribution standard, and
> nothing was rescued or manufactured. Extend it to the life-loops next — not for tidiness, but because F4 adds
> relational episodes into a now-time-normalised childhood, and if those episodes still accrue per-episode the
> two contributions of a single run are on different clocks, which is exactly the "differ only in relational
> content" guarantee F4 was built on. Verify that first, extend, then re-measure F3 — its +3.73 is a live claim
> still resting on an episode count. Sweeps and the register pass after.**
