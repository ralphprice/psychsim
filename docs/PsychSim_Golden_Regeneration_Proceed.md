# Golden regeneration — PROCEED. **One requirement, one flag.**

**Confirmed, no redirect. The sequencing is right and you are doing it the way the Stage-3 failure taught.
Two things to carry into it.**

---

## 1. The requirement — every delta must be ATTRIBUTABLE, not just annotated

**A golden regeneration is the single highest-risk moment for the honesty wall, because it bakes in whatever
the current state is. Whatever is wrong at the moment of regeneration stops being detectable.** That is exactly
why the Stage-3 regen mattered, and naming the causes in a note is necessary but not sufficient.

> **The check that makes a regeneration honest rather than a reset: inspect the diff and trace EACH shift to
> one of the five named causes (read-out re-derivation, harsh-mirror routes, `age_window`, domain-count
> dilution, Lump #13). A delta that does NOT trace to a named cause is an undiagnosed change about to be made
> invisible — diagnose it BEFORE baking it in, not after.**

**And specifically: no classification should flip without its own explanation.** If one does, it needs naming
and attributing in the note like any other delta. **The note should record not just "these things changed the
baseline" but "this shift came from that cause" — attribution, not just a list.**

## 2. The flag — the regenerated golden is the reference for re-verifying the four emergent findings

**The audit ruling's final step was: re-verify the four emergent findings (Gross reappraisal/suppression,
Tremblay aggression trajectory, Buckholtz reward-hypersensitivity, adolescent reward-seeking) on corrected
instruments.** The regenerated golden IS the corrected-instrument reference state.

**Do not fold that into this regeneration** — it is its own task and it deserves its own measurement. **But flag
it as the natural next-after:** once the golden captures the corrected state, the four findings can be
re-measured against it, and the honest expectation stands as ruled — the dynamics-measured ones (Tremblay, the
provocation-specificity) should survive; any that existed only as observer-construct scores may not. **That
re-verification is what closes the audit.**

## 3. On the cycle summary — one refinement worth carrying to the thesis

Your framing is right and the through-line is now properly registered. **One thing worth stating precisely,
because it will matter in writing:** the flagship test's result — the seed→construct tautology collapsing ~6×
while the test survives on a small real residual — is **a quantified decomposition, not a verdict.** Not "the
test was an identity" and not "the test was fine": **most of the effect was the double-count, and a small real
component remains.** That precision is worth more in a thesis than either clean verdict, and it is only
available because the instrument was corrected and re-measured rather than argued about.

---

**Proceed with the regeneration under the attributability check. Then the substantive queue — the DA second
front, the adult-plasticity floor, `punishment_sensitivity` → the yoked probe — and the emergent-findings
re-verification when you reach it.**

> **Regenerate, but attribute: a note that lists causes is annotation; a note that maps each delta to its cause
> is verification, and only the second one keeps the baseline honest. Nothing unattributable gets baked in.
> Then the regenerated golden becomes the reference against which the four emergent findings are re-verified —
> the step that actually closes the audit.**
