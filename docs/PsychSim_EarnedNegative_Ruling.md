# The earned negative — RULING: **un-xfail it.** Commit and push.

**Your lean is right. Here is the argument that settles it, which neither of us has made yet.**

---

## 1. ★ §18's bias is DIRECTIONAL — and it has just changed sides

**§18: artifacts inflate. So every effect size on this substrate is an OVER-estimate.**

- When the divergence read **0.0755 — above the line** — the bias said *"this may be inflation; do not retire
  the finding."* **We held it. We were right: two grounded completions shrank it.**
- It now reads **0.0335 — below the line.** **The same bias now says the TRUE value is even LOWER than
  0.0335.**

> **An earned negative measured on an inflating substrate holds *a fortiori*.** The caution that kept the
> finding open is now the thing that confirms it. **§18 asymmetrically protects negative findings — and this
> is the first time that has mattered.** Record it.

**So un-xfailing is not "accepting a result because it went our way." It is the only reading consistent with
the caution we have been applying against ourselves for three phases.**

## 2. ★ And the §18 arc is no longer a caution. It is a measured law.

**0.0755 (saturation) → 0.0534 (`dACC-GABA`) → 0.0335 (`PGi`).** **Three grounded completions, three
shrinkages, monotone, on one quantity.** That is not three anecdotes with a shared direction — **it is a
trend with a mechanism**, and it was predicted before it was observed.

**So the un-xfailed test now carries a PREDICTION, and you should write it into the test:**
> **The remaining completions — the 8 cortical brakes, `vlPAG`'s drive, `PGi`'s exteroceptive arm — should
> shrink it FURTHER. If any grounded completion makes it GROW, that is the surprise §18 says to stop for, and
> the test going red is the signal.**

**That converts the earned negative from a pin into an instrument.** It no longer merely records that the
divergence does not emerge; **it predicts the direction of every future completion, and it fails loudly if the
substrate disagrees.**

## 3. The resolution condition's lesson — record it

You wrote *"not before the cortical brake layer is complete."* **It fired early because the condition named a
GUESS ABOUT WHICH CHANGE WOULD ANSWER THE QUESTION, rather than the QUESTION.** The real mechanism was
general — *any* grounded completion — and we had already written that down as §18 in the same breath.

> **RULE: a resolution condition names the QUESTION, not the mechanism you expect to answer it.** *"When the
> brake layer is complete"* was a hypothesis wearing a condition's clothes. *"Re-measure after any grounded
> completion; the finding holds while it stays below 0.05"* would have been the condition — **and it would
> have fired exactly when it did, for exactly the right reason.**

**And note what saved us: the mechanism was strict, so it fired anyway.** A non-strict xfail would have sat
green and silent while the finding it guarded was confirmed by three completions and nobody noticed. **The
strictness caught our own bad condition.**

## 4. Your `PGi` self-diagnosis is right — and door 1 is correctly still open

> *"dissolved under direct pain (72 %→37 %) but survived under vicarious distress (73 %→64 %)… the failure
> mode recursed one synapse down."*

**Caught by you, on your own build, before anyone asked. And it is the third recursion:** LC completed with
one afferent → `PGi` specced without asking whether *it* would be afferented → **`PGi` built with one arm.**
**Each completion is partial, and each partiality is invisible until measured.**

**So the failure mode needs a STOPPING RULE, because "complete everything" is the whole brain:**

> **Completion is recursive. The stopping rule is not anatomical — it is the MEASUREMENT, taken IN THE
> CONTEXT THE CLAIM IS MADE IN.** Complete until **no single input carries the claim**, and test it **where
> the claim lives.** **The CU study's claim lives in the vicarious context. So door 1 closes when the
> entailment dissolves under vicarious distress — not under direct pain, and not on the anatomy looking
> tidy.**

**64 % is not a closed door. Your read is correct.** `PGi`'s **exteroceptive/salience arm** is next — Van
Bockstaele/Aston-Jones 1993 name PGi as the integrator of **exteroceptive *and* interoceptive** input, so the
arm is grounded and the diagnostic is yours.

---

## Finalize
**Un-xfail** (with the prediction written into it) · commit · push. **`CeA → LC` untouched at `moderate` ·
LC's and `PGi`'s sets recorded INCOMPLETE with the remainder named · `PBN`/`NTS` re-marked WRONG-TARGET · the
exclusion-is-a-claim gap (`FRUSTRATION`) registered.**

**Then: `PGi`'s exteroceptive arm — diagnostic first. Door 1 stays partial. #2 stays open on doors 2 and 3
regardless, and door 1's numbers under direct pain are exactly the kind of thing that would look like a
resolution.**
