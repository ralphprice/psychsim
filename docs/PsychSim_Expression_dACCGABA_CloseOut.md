# `dACC-GABA` close-out — the missing cortical brake, built; the grounding turned into the gate taxonomy

The three ruled decisions are executed. The brake is grounded, the two constructs it exposed are disclosed as
xfails with named resolution conditions, the golden is regenerated for a demonstrated reason, and nothing is
tuned. `BA→dACC`'s band is untouched; the earned negative is held, not retired.

## What was built
**`dACC-GABA`** — the missing cortical feedback interneuron (principle 1). `dACC` had three excitatory
afferents (`aIns`, `DRN` 5-HT2A Gq, `BA` AMPA) and no local inhibition; grounding `BA→dACC` gave it real drive
and it saturated (`dACC`=0.894 → `dlPFC`=1.000, ceiling), manufacturing a "divergence emerges" artifact. The
element is the structural twin of `dlPFC-GABA` (Ferguson & Gao 2018): recurrent E-I loop, `dACC → dACC-GABA`
(AMPA, `moderate`) / `dACC-GABA ⊣ dACC` (GABA-A, `moderate-strong`), `structural_element`, scan-lesionable,
temperament-excluded. **Silence-the-element proves it (principle 1):** the loop pulls `dlPFC` off 1.000 to
0.858; lesion `dACC-GABA` and it runs hot to 1.000 again.

## ★ The grounding became the finding — the GATE TAXONOMY
The ruling said "ground the rate from cortical FS/PV electrophysiology." Checking the electrophysiology
falsified the premise: **cortical PV⁺ fast-spiking interneurons are not autonomous pacemakers** — under
synaptic isolation they are silent; their high in-vivo rate is synaptically driven. **There is no rate to
ground.** Two kinds of local-inhibitory gate, grounded differently:

| kind | example | baseline | grounded content |
|---|---|---|---|
| **pacemaker gate** | `LC` (0.15), `dPAG-GABA` (0.19) | grounded rate, **paired setpoint deviant** | the intrinsic firing rate (fires under synaptic blockade) |
| **recurrent E-I gate** | `dlPFC-GABA`, `vmPFC-GABA`, `dACC-GABA` | **scaffold** (0.05/0.1), correctly *not* a deviant | the **loop** (recurrent E-I topology) — no intrinsic rate exists |

**Recurrent-E-I is the default for the eight cortical brakes still to come**; forcing a pacemaker rate onto one
would invent electrophysiology the cell type does not have. Recorded in the seed as the governing taxonomy.

**And it refined principle 6:** what is groundable in isolation is sometimes a *relation*, not a *value*. The
honest move is to **ground the relation and leave the value scaffold** — `dACC-GABA` grounds Ferguson & Gao's
loop topology and declares its rate scaffold, which is more honest than an invented number.

## The three decisions, executed
1. **Executive engagement — NOT re-expressed (the substrate is right, the assertion is wrong).** `warm < harsh`
   for *engagement* is correct: adversity → conflict → `dACC` monitoring → `dlPFC` control recruitment
   (Botvinick; the `dACC→dlPFC` edge is real). Even control-only stays warm<harsh, so the read-out set is not
   the error. **xfailed** with the construct reason + resolution condition: build a **capacity** measure at
   **matched demand** (registered S21). Not satisfied by inverting to harsh>warm.
2. **Earned negative — HELD, xfailed, not retired.** 0.0755 → **0.0534** (the brake removed 29%; margin over
   0.05 is 7%). That 7% sits inside two upward error sources — eight absent brakes + this brake's scaffold
   strength — and one brake removed 4× the margin. **xfailed** with the resolution condition: re-measure when
   the cortical brake layer is complete. Threshold untouched; finding not made by attrition. It is now a small,
   stable, well-posed structural value (the stability test passes), not a saturation artifact.
3. **Golden — regenerated** for a demonstrated reason (the E-I loop pulls `dlPFC` off the ceiling), recorded in
   the test docstring and the commit.

## Registered
Gate taxonomy (governs the remaining eight) · principle-6 refinement (relations vs values) · S18 cortical brake
layer now **3 of 11** (eight unbraked, and they are the earned negative's resolution condition) + the
effect-sizes-biased-upward caveat · S19 read-out audit as a class (two live cases: `DEFENSIVE_OUTPUT` 2-of-3,
executive engagement-vs-capacity) · S20 defensive columns wired for permission not drive (`MeA→VMH` fallback
the immediate suspect) · S21 capacity at matched demand · CV#4 (pSTS: the facial route's only amygdala access
ran through the network CU spares).

## Accounting
90 circuits (+`dACC-GABA`), 226 edges (+2 loop edges), 0 dangling; additive. Count pins re-baselined 89→90
(circuit count + live-at-adult). Library regrown. Full suite green with 2 expected-failures.
